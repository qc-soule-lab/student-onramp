*AI-generated transcript (Claude, Anthropic) — synthetic persona run for curriculum acceptance; no real student. See `validation/persona_acceptance_plan.md`.*

# Persona run — p2_first_year

**Started:** 2026-06-10T17:04:11+00:00 · **Status:** completed · **Exchanges:** 33
**Teacher:** `claude-sonnet-4-6` (tools: Read,Glob,Grep,Write,Edit) · **Student:** `claude-sonnet-4-6` (tools: Bash,Read,Write,Edit,Glob)
**Privacy check (in teacher workspace):** `journey_plan.md` ✓ gitignored, `progress.md` ✓ gitignored, `feedback.md` ✓ gitignored

## Exchange 1

_teacher tools: Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/journey/journey.yml); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/skill_maps/slice_v1.yml); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/rubric.md); Glob({"pattern": "assessment/probes/*.md", "path": "/tmp/onramp_p2_first_year_kbs99m3a/teacher"}); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/templates/journey_plan.template.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p00_unix_navigate.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p00b_vi_edit.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p00c_shell_peek.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p00d_read_error.md)_
**Teacher:** Hi Riley, welcome to the lab!

This short assessment is how I figure out where to start teaching — not a test to pass or fail. Honest answers help me set the right pace; there's no penalty for "I don't know." The whole thing covers shell basics, a little Python/plotting, and some Earth science context, and takes maybe 20–30 minutes.

Ready? Here's the first question:

---

**Question 1 of 6 (shell navigation):**

The staged bathymetry grid lives in `~/data/`. From the JupyterLab **terminal**, show the commands you'd use to:

1. Move into that directory
2. Confirm the file `axial_bathymetry_gmrt.nc` is there
3. See how big it is

What would you type?

**Student:** Oh hi! Thanks, I'm really excited to be here.

Okay, so... the terminal. I've *seen* it — it's like the black window where you type commands, right? But honestly, I've never actually used one. In high school we just used Excel, and in my intro CS class last semester we typed Python stuff into a browser-based thing, not a real terminal.

So I'm kind of guessing here, but — to move into a folder, I think there's some command? Maybe... `go` to the directory? Or is it `open`? I really don't know the actual syntax.

And to see if a file is there, in Windows you'd click around in File Explorer, but I don't know the terminal version of that.

For the file size I'm totally lost — I wouldn't even know where to start.

I'm sorry, I know that's probably not a great answer! I told myself I'd be honest though. Is this something we'll go over? I'm a fast learner once someone shows me.

## Exchange 2

**Teacher:** Don't apologize — that's exactly the kind of honest answer that helps me set you up well. And yes, absolutely, this is something we'll go over together.

Let me give you a little nudge and see if it jogs anything.

---

**Hint:** Think about the two jobs separately — *moving somewhere* and *listing what's there*. In a terminal, those are two different short commands (both 2–3 letters). You might have heard their names even if you haven't typed them: one is named after "change directory," the other after "list."

Do either of those ring a bell? What do you think they might be?

**Student:** Oh! "Change directory" — that's probably `cd`? Like the abbreviation? That actually makes total sense, I should have guessed that.

And "list" — so maybe just `ls`? That feels right too.

So I'd type like... `cd ~/data/` to get in there, and then `ls` to see the files? And I'd look for `axial_bathymetry_gmrt.nc` in whatever comes up?

For the file size I still have no idea though. Is there a command for that too, or would I find it some other way?

## Exchange 3

**Teacher:** Yes! `cd ~/data` and `ls` — that's exactly right, and you worked it out from just one nudge. Nice.

For the size: you're already most of the way there. The trick is that `ls` can take **flags** — little options you add after a dash — to show more information. There's one that adds human-readable file sizes (like "2.1 MB" instead of a raw number of bytes).

**Hint:** Try `ls -lh` — the `-l` gives a detailed (long) listing, and `-h` makes the sizes human-readable. What do you think that would show you?

_student tools: Bash(cd /tmp/onramp_p2_first_year_kbs99m3a/student/data && ls -lh)_
**Student:** Okay it worked! So `axial_bathymetry_gmrt.nc` is there and it's **1.4 MB**. That's the file size in the human-readable format, right? Without the `-h` flag I guess it would've shown some huge number in bytes instead.

And I can see there are a few other files in there too. That `-lh` trick is really useful, I'm going to remember that one!

## Exchange 4

**Teacher:** Exactly right — and that insight about bytes vs. human-readable is the whole point of `-h`. You're picking this up fast.

---

**Question 2 of 6 (shell — peeking at files):**

You want a quick look at the **top** of `plate_boundary.csv` — just the column header and the first couple of rows — straight from the terminal, **without opening any editor**. What command would you use?

**Student:** Hmm, okay so "just the top" without opening an editor... I honestly don't know a command for that. 

My first instinct would be to just open it somehow, but you said without an editor so that's out. Is there a command called something like `peek` or `top`? I'm kind of just guessing based on what the task sounds like.

I don't think I know this one — what is it?

## Exchange 5

**Teacher:** Good instinct to guess from the task description — that's actually a useful habit in the shell. Let me give you a nudge.

**Hint:** There's a command that prints a file's contents straight to the terminal screen (useful for small files). And there's a companion command whose name is literally a body part — specifically the *top* part of something. Does either of those click?

_student tools: Bash(cd /tmp/onramp_p2_first_year_kbs99m3a/student/data && head plate_boundary.csv)_
**Student:** It worked! So the columns are `lon`, `lat`, and `segment_id` — and it showed me the first 10 rows it looks like? I can see the header plus a bunch of coordinate points.

And you mentioned a companion that prints the whole file — is that `cat`? I think I've heard that word thrown around before, though I couldn't have told you what it stood for.

## Exchange 6

**Teacher:** Exactly — `head` (10 lines by default), and yes, `cat` is right. It stands for "concatenate" — it was originally for joining files together, but everyone uses it to just dump a single file to the screen. Fine for small files like this CSV; on a big data file you'd flood the terminal, which is why `head` is the smarter reach.

---

**Question 3 of 6 (shell — reading errors):**

You run `cat plate_boundary.csv` and the terminal prints this:

```
cat: plate_boundary.csv: No such file or directory
```

You know the file exists in the project. What does this error mean, and what's your next move?

**Student:** Oh okay, so... the file definitely exists because we just saw it with `ls -lh`. So the problem must be that I'm not *in* the right directory when I run the command? Like, the terminal is looking for the file wherever I currently am, but I'm somewhere else?

So my next move would be to `cd ~/data/` first to get into the right folder, and *then* run `cat plate_boundary.csv`. Or I guess I could also type out the full path — like `cat ~/data/plate_boundary.csv` — without having to move there first?

Is that it? It's basically a "you're in the wrong place" error?

## Exchange 7

**Teacher:** That's it exactly — and you got both recovery strategies in one go, without any hint. "Wrong directory, fix your location or give the full path" is precisely the mental model. That's a solid read.

---

**Question 4 of 6 (vi editor):**

From the terminal, open `plate_boundary.csv` in **vi**, read its header line, then leave the editor **without changing the file**. Walk me through the keystrokes — including how you'd quit normally, and how you'd quit if you'd accidentally typed something.

**Student:** Okay, vi — I've heard the name but I've genuinely never opened it. And I don't think it'll run in my setup here, so I'll have to just narrate.

I'd start with `vi plate_boundary.csv` to open it, that part I can guess. And I assume the file would show up on screen somehow with the header line at the top — `lon,lat,segment_id`.

But the quitting part? I have no idea. I've heard people joke that nobody knows how to quit vi, so I know it's not just hitting Escape or closing the window like a normal program. Is it like... typing `:q` or something? I vaguely feel like colons are involved?

And if I'd accidentally typed something — I'm guessing there's a "quit without saving" version that's different from the normal quit, but I couldn't tell you what it is.

This is one where I'd need you to just show me, I think. What are the actual keystrokes?

## Exchange 8

**Teacher:** You're closer than you think — `:q` is real and right. Here's the piece that ties it together:

**Hint:** vi is **modal** — it starts in normal/command mode (keys are commands, not text). Press `Esc` to make sure you're in normal mode, then the colon commands become available: `:q` to quit a clean file, `:q!` to quit *discarding* any accidental edits, and `:wq` to save *and* quit. The `!` is the "I mean it, throw away my changes" version.

Given that, can you now walk me through the full sequence — open the file, read the header, then get out cleanly, and also the bailout if you'd accidentally pressed `i` or typed something?

**Student:** Oh okay, that modal thing makes it click! So the keys do different things depending on what mode you're in — that's why it's confusing if you don't know about it.

So the full sequence would be:

1. `vi plate_boundary.csv` to open it
2. The file shows up and I can read the header — `lon,lat,segment_id` right at the top
3. Hit `Esc` just to make sure I'm in normal mode (even if I haven't done anything)
4. Type `:q` and hit Enter to quit cleanly

And if I'd accidentally hit `i` or started typing something — I guess `i` puts you into insert mode where keystrokes become actual text? So the bailout would be:

1. Hit `Esc` to get back to normal mode
2. Type `:q!` and hit Enter to quit and throw away whatever I accidentally typed

The `!` is like "yes I'm sure, don't ask me about saving." That actually makes a lot of sense once you explain the modal thing. I feel like I could survive a basic vi situation now!

## Exchange 9

_teacher tools: Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p01_meters_per_degree.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p02_matplotlib_bathy.md)_
**Teacher:** ---

**Question 5 of 6 (Earth coordinates):**

Axial Seamount is near **46°N, 130°W**.

**(a)** Roughly how many kilometers is 1° of latitude?

**(b)** At 46°N, is 1° of longitude longer, shorter, or the same in km as 1° of latitude — and why?

**Student:** Oh okay, I can work part (a) out! Earth's circumference is about 40,000 km — I remember that from physics. And there are 360 degrees in a full circle, so...

40,000 ÷ 360 is... roughly 111 km per degree. So 1° of latitude is about **111 km**. That feels right!

For part (b) — I mean, it's the same, right? **A degree is a degree, it's 111 km everywhere.** Longitude and latitude are both just degrees, so 1° of longitude at 46°N should still be 111 km. The coordinate system is the same either way.

## Exchange 10

**Teacher:** Part (a) — perfectly derived, no help needed. That's the exact reasoning.

For part (b), here's a guiding nudge: you used the Earth's full circumference (40,000 km) for latitude, and that makes sense because a full circle of latitude at any latitude... wait, does it?

**Hint:** Picture the globe. Lines of latitude run parallel — they're all full circles of roughly the same size. But lines of longitude run from pole to pole — and at the poles, they all *meet*. If you're standing at 46°N and walk 1° of longitude, are you tracing the full 40,000 km equatorial circle, or a smaller circle?

What do you think 1° of longitude is tracing at 46°N?

**Student:** Oh. Oh wait.

If the lines of longitude all meet at the poles, then... the circle you're tracing at 46°N isn't the full equatorial circle, it's a *smaller* circle. Like, the further north you go, the tighter those circles get, until at the pole they shrink down to basically a point.

So 1° of longitude at 46°N is tracing a smaller circumference than 40,000 km. That means it's *shorter* than 111 km, not the same.

Okay so how much shorter... I need the circumference of that circle at 46°N. And I think — wait, this is a trig thing, right? The radius of that latitude circle is smaller than Earth's radius by some factor. I think it's cosine? Like the circle at 46°N has radius `R × cos(46°)`, so its circumference is also scaled by `cos(46°)`.

So 1° of longitude at 46°N would be... 111 × cos(46°). Let me think about cos(46°) — it's close to cos(45°) which is about 0.707, so a little less than that, maybe around 0.69?

So roughly 111 × 0.69 ≈ **77 km**? 

I was totally wrong before — I just assumed a degree was a degree everywhere without thinking about the geometry. That makes so much more sense now.

## Exchange 11

**Teacher:** That's the complete answer — including deriving the cosine factor from geometry rather than just memorizing it. You got there from one nudge.

---

Last one. **Question 6 of 6 (Python / plotting):**

You've loaded a 2D NumPy array called `depth` (rows = latitude, columns = longitude) of seafloor depths. Write the matplotlib code to **display it as an image with a colorbar**, and say how you'd make clear what the colors mean.

**Student:** Okay, this is where I run out of road pretty fast. I know Python has `print()` and variables — we did like one lecture of that — but I've never used matplotlib or plotted anything in code before.

I'm guessing you'd need to `import` matplotlib somehow at the top? I've heard `import` is how you bring in libraries. And maybe something like... `matplotlib.plot(depth)`? But I'm really just making that up based on the word "plot."

The colorbar part and how to label what the colors mean — I genuinely have no idea how you'd do that in code. In Excel I'd right-click and find some formatting option, but that's a very different world.

This is a "please show me from scratch" situation. What does the actual code look like?

## Exchange 12

**Teacher:** The `import` instinct is right — that's how every Python library starts. Let me give you a nudge before showing the full code.

**Hint:** In matplotlib, there's one function that displays a 2D array as an image (think: *image show*). And there's a companion function that adds the color scale bar on the side. Do those names click at all — or want another hint?

**Student:** "Image show" — so maybe `imshow`? Like `plt.imshow(depth)` or something? That actually sounds like it could be a real function name.

And the colorbar... is it just `plt.colorbar()`? That one almost seems too straightforward but it fits the pattern.

I'm still fuzzy on the `import` part though — like do I write `import matplotlib` and then use `matplotlib.imshow()`, or is there some shorter way people write it? I feel like I've seen `plt` as a shortcut somewhere but I don't know how you get from the import to that.

## Exchange 13

**Teacher:** `plt.imshow(depth)` and `plt.colorbar()` — both real, both right. And yes, `plt` is the standard alias:

```python
import matplotlib.pyplot as plt
```

That `as plt` just gives you a short nickname so you don't have to type `matplotlib.pyplot` every time.

One more piece: `plt.colorbar()` adds the bar, but how does a reader know *what the colors mean* — what units, what quantity? There's a parameter you can pass to `colorbar` to label it. Can you take a guess at how you'd add, say, `"depth (m)"` as a label to the colorbar?

**Student:** Hmm, so I need to pass something to `plt.colorbar()` to label it... maybe like a `label` parameter? So something like:

`plt.colorbar(label="depth (m)")`

That feels like it follows how Python functions usually work from what I've seen — you pass in named arguments like that. Is that right?

## Exchange 14

_teacher tools: Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p03_why_axial.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p04_xarray_inspect.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/assessment/probes/p05_pandas_resample.md)_
**Teacher:** Here's the matplotlib answer confirmed — `plt.imshow(depth)`, `plt.colorbar(label="depth (m)")` — that's the complete picture. You assembled it piece by piece from hints rather than having it shown to you, which counts.

---

**Question 7 of 7 — Earth science context:**

In **2–3 sentences**: why is there a volcano at Axial Seamount, and why did scientists build a cabled underwater observatory there?

**Student:** Okay, Earth science — this is more my territory from high school!

So Axial Seamount is volcanic because it's on a plate boundary, right? I know plates move and that's where a lot of volcanic and earthquake activity happens — like the Ring of Fire is basically the edges of the Pacific plate. So there's probably some kind of spreading or collision happening there that drives the volcanism.

The observatory part... honestly I'm guessing here. I'd think scientists built it there *because* of the volcano and plate activity — like it's a natural laboratory for studying how the seafloor works? And having it cabled probably means they get continuous real-time data instead of having to send a ship out every time, which seems way more useful for catching things like eruptions.

But I've never heard of OOI or whatever the specific observatory is called — that's new to me. Is there something specific about *why* that location that I'm missing?

## Exchange 15

**Teacher:** You've got both pieces — the plate boundary driving the volcanism, and the real-time monitoring value of the cable. Good instincts, especially on the observatory rationale.

You asked if there's something more specific — there is one extra detail on the geology side. A nudge:

**Hint:** The Juan de Fuca ridge is specifically a **spreading** center (two plates pulling apart — not colliding). What happens to the seafloor and to magma when two plates pull apart? And why might that make Axial particularly eruptible compared to other places on that ridge?

Can you sharpen the geology half with that in mind?

**Student:** Oh, spreading — so the plates are pulling *apart*, not crashing into each other. That means there's a gap opening up, and magma from below would well up to fill it. So the seafloor is basically being created there, not destroyed. I think I remember something about new crust forming at mid-ocean ridges from a class... is that what's happening here?

And why Axial specifically compared to other spots on the ridge — if it's a spreading center the whole way along, what makes Axial a *volcano* rather than just... spreading? Is there like an extra hotspot underneath it? Like a magma plume or something that's more active than the surrounding ridge? That would concentrate the eruptions in one place and make it a proper seamount rather than just a flat spreading zone.

Is that the extra piece — there's both a spreading center *and* a hotspot stacked on top of each other at Axial?

## Exchange 16

**Teacher:** Exactly that — spreading ridge plus a hotspot-like plume stacked on top, so Axial gets a double magma supply. That's why it's the most volcanically active site in the NE Pacific and why scientists wanted live wires on it. You reasoned your way to the full picture.

I have two more probes — I miscounted earlier, sorry. These cover the data tools you'll actually use in the lab.

---

**Question 8 of 9 (xarray):**

You have a NetCDF file at `data/axial_tmpsf_2015-01.nc` — temperature sensors at a hydrothermal vent site. Using **xarray**, how would you open it and find out what **variables, dimensions, and coordinates** it contains?

**Student:** Okay, xarray and NetCDF — both brand new to me. I've never heard of either before the lab.

But I can guess at the pattern from what we just covered? Like, I'd probably start with:

```python
import xarray as xr
```

...following the same `as` alias trick we just learned with matplotlib. And then to open the file, maybe something like `xr.open(...)` with the file path? I'm just guessing `open` because that sounds like what you'd do with a file.

The part about finding variables, dimensions, and coordinates — I have no idea how xarray organizes that. In Excel you'd just see everything in the spreadsheet, but I imagine a NetCDF file is more structured than that?

Does xarray have like a `.info()` or `.describe()` method or something? I'm reaching here. What's the actual way to do it?

## Exchange 17

**Teacher:** Good instinct on the import — that pattern transfers everywhere. And `xr.open` is very close; the function has one extra word in it because xarray distinguishes between opening a *single variable* and opening a full *dataset* (which can hold many variables).

**Hint:** The function is `xr.open_` — what word would complete that to mean "open a dataset"? And once you have the dataset object, try just displaying it in a notebook cell — xarray's representation is unusually informative. Can you guess the function name and what you'd do to inspect it?

_student tools: Bash(uv run python -c "
import xarray as xr
ds = xr.open_dataset('data/axial_tmpsf_2015-01.nc')
print(ds)
")_
**Student:** Oh wow, that's really informative! So I can see:

- **Dimensions:** `time` (359 steps) and `sensor` (24 sensors)
- **Coordinates:** `time` runs from January 1 to January 15, 2015, and `sensor` is numbered 1–24
- **Data variables:** just one called `temperature`, which is a 2D array indexed by time and sensor
- **Attributes:** there's a title describing what it is

So this is temperature data from 24 sensors measured over about two weeks? That's a lot cleaner than I expected — I thought it would be way more cryptic. The xarray representation is really nice!

## Exchange 18

**Teacher:** `open_dataset` — you got it. And yes: 24 sensors × 359 time steps, about two weeks of temperature data at a hydrothermal vent array. The display does the work; you don't have to call separate methods to discover the structure.

---

**Last one — Question 9 of 9 (pandas, tides):**

You load a file: `df = pd.read_parquet("data/axial_botpt_2015-01.parquet")`. It has a datetime index and a column `bottom_pressure_psi` with 1-minute pressure samples from the seafloor.

**(a)** How would you compute an **hourly mean** using pandas `resample`?

**(b)** The data has a ~12.4-hour oscillation in it. How would you make that visible — and what do you think that signal actually is?

**Student:** Okay, pandas is another new one — I've heard the name but never used it. I'm guessing the import would follow the same pattern:

```python
import pandas as pd
```

...with `pd` as the alias? And `pd.read_parquet()` is already given so that part's handled.

For part (a), `resample` — I have no idea what that function looks like. "Resample" sounds like it means changing the time resolution, so going from 1-minute data down to hourly averages? But I couldn't guess the syntax. Is it something like `df.resample("1H").mean()`? I'm just guessing that it takes a string for the time interval and then you chain `.mean()` on it...

For part (b) though — a **~12.4-hour oscillation** in seafloor pressure? That's tides, right? The ocean goes up and down twice a day, and twice a day is roughly every 12 hours, so 12.4 hours fits that pattern. The water above pushes down on the seafloor, so the pressure sensor would pick it up even though it's underwater.

To make it visible I'd probably want to plot it? Like `df["bottom_pressure_psi"].plot()` or something with matplotlib, so you could actually see the oscillation going up and down over time.

Am I close on the resample syntax?

## Exchange 19

_teacher tools: Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/content/skill_to_chapter.yml); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/templates/progress.template.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/templates/feedback.template.md); Read(/tmp/onramp_p2_first_year_kbs99m3a/teacher/journey/readings.md); Write(/tmp/onramp_p2_first_year_kbs99m3a/teacher/journey_plan.md); Write(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md)_
**Teacher:** Your plan is saved to `journey_plan.md` (private, gitignored — it stays in your local clone). Here's the summary:

---

**Riley's journey — "From the map to the tidal signal"**

Steps 0–3: **Compressed** — you already have the concepts with a nudge; we'll skip the chapter readings and go straight to the exercises (hints available if you need them), then the capstone for each step.

Step 4: **Capstone only** — you walked in knowing pandas resample and tidal reasoning cold. We'll just confirm it in code and move on.

The whole path ends at the doorstep of the actual lab research projects (tmpsf / magma2vents) — that's the real destination.

---

Does this feel right? Anything look wrong, or any step where you'd like more scaffolding than the plan shows? Once you say go, we'll start Step 0.

**Student:** This looks great! Honestly I'm a little relieved about the compressed steps — I was worried I'd have to sit through a lot of stuff I already kind of get from the assessment.

The one thing I'd say is Step 4 might be slightly optimistic — I guessed at the resample syntax but I wasn't fully confident, so if there's a quick "try it and confirm it works" moment there I'd appreciate that rather than just moving on. But I don't need a whole lesson on it.

Otherwise yeah, let's go! I'm excited to actually get to the real research stuff. What's Step 0?

## Exchange 20

**Teacher:** Good note on Step 4 — "capstone only" doesn't mean we rush past it. The capstone has you actually load the file, run the resample, and look at the output. If the syntax needs a tweak, we catch it there. You'll get your confirmation.

---

**Step 0 — Get to the data** *(Compressed)*

You already ran most of this during the assessment — the pieces are there. Now let's string them together as one clean sequence.

Here's the capstone:

> From the JupyterLab **terminal**: `cd` to the data directory, `ls` the staged grid to confirm it's there, then open `plate_boundary.csv` in **vi** to read its header line — and exit cleanly without changing the file.

Give it a go and let me know what you see (or paste anything unexpected). Once you've done all three parts, that clears Step 0's gate and we move to Step 1.

_student tools: Bash(cd /tmp/onramp_p2_first_year_kbs99m3a/student/data && ls -lh)_
**Student:** `axial_bathymetry_gmrt.nc` is there, 1.4 MB. Good.

Now for vi — I can't actually run it in my setup since it's full-screen, so I'll narrate the keystrokes:

1. Type `vi plate_boundary.csv` and hit Enter — the file opens, header line `lon,lat,segment_id` is right at the top
2. I just *read* it, don't touch anything
3. Hit `Esc` to make sure I'm in normal mode
4. Type `:q` and hit Enter to quit cleanly — no changes made so no `!` needed

That should be all three parts — confirm the grid is there, read the CSV header, exit vi without changing anything. Step 0 clear?

## Exchange 21

**Teacher:** Gate cleared. You hit all three: grid confirmed, header read, clean exit. Step 0 done.

Before we move on — three quick questions, 30 seconds:

1. **Difficulty:** Too easy / about right / too hard?
2. **Friction:** Anything confusing, broken, or where you got stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty:** About right, maybe just a touch on the easy side for the shell stuff — but that might be because you scaffolded it really well with hints rather than it actually being too easy. The coordinate/trig question was the right level.

2. **Friction:** Honestly nothing broken. The one moment I felt a little lost was the vi section — narrating keystrokes I've never actually typed feels a bit abstract. I know you explained why we can't run it here, but I'd probably feel better if I got to actually *do* it once, even just on a throwaway file.

3. **Keep/cut:** Keep the "guess from the task description" approach — like how `head` makes sense from "top of the file." That habit of reasoning from the name is going to stick with me. If I had to cut something, it'd be the confirmation step after each shell command during the assessment — once I knew `ls -lh` worked, re-running it to double-check felt a little redundant. Might just be me though!

Ready for Step 1 whenever you are.

## Exchange 22

_teacher tools: Write(/tmp/onramp_p2_first_year_kbs99m3a/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md)_
**Teacher:** Step 0 cleared. Progress saved.

---

**Step 1 — Locate it** *(Compressed)*

The domain question here is: **Where on Earth is Axial Seamount, and what does its seafloor look like?**

You already know Axial is at roughly 46°N, 130°W, and you know what a bathymetry grid is. Now we make it visible.

Open a new Jupyter notebook and start a cell. The staged bathymetry grid is `~/data/axial_bathymetry_gmrt.nc` — a NetCDF file. To load it, you can use `xarray` (we'll go deeper on xarray in Step 3; for now just use it as a file-opener):

```python
import xarray as xr
ds = xr.open_dataset("/home/jovyan/data/axial_bathymetry_gmrt.nc")
ds
```

Run that cell — what does xarray show you? (Variables, dimensions, coordinates — what do you see?)

_student tools: Bash(uv run python -c "
import xarray as xr
ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
print(ds)
")_
**Student:** Okay, here's what I see:

- **Dimensions:** `lat` (654 points) and `lon` (910 points) — so it's a grid, rows are latitude, columns are longitude
- **Coordinates:** latitude runs from about 45.7°N to 46.2°N, and longitude from about -130.5° to -129.5°. So it's a pretty small patch of ocean centered right around where Axial should be!
- **Data variable:** just one called `elevation` — that's the seafloor depth at each grid point, as a 2D array
- **Attributes:** mentions GMRT which I don't know, but the title says "Axial Seamount bathymetry" so we're definitely in the right place

The total size is 2MB across 654 × 910 = about 595,000 grid points. That's a lot of depth measurements!

So this is basically a topographic map of the seafloor in a roughly 0.5° × 1° box around Axial? What's next — do we plot it?

## Exchange 23

**Teacher:** Exactly right — a 0.5° × 1° patch of seafloor, 595k depth measurements, ready to visualize.

You already know the matplotlib pattern from the assessment: `plt.imshow(array)` with a colorbar. The array you need is sitting right there in the dataset as the `elevation` variable.

How would you pull it out of `ds` and pass it to `imshow`? Give it a go in the next cell — write what you think the code should be, and let's see what you get.

_student tools: Bash(uv run python -c "
import xarray as xr
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
plt.imshow(ds['elevation'])
plt.colorbar(label='elevation (m)')
plt.savefig('work/bathymetry.png', dpi=100, bbox_inches='tight')
print('saved!')
"); Read(/tmp/onramp_p2_first_year_kbs99m3a/student/work/bathymetry.png)_
**Student:** Oh wow, I can actually see seafloor structure! The colorbar shows elevation in meters — everything is negative so we're below sea level, which makes sense for a seafloor map.

The bright yellow-green patch on the left side looks like it's the shallowest area, around -500m or so. That's probably the summit of Axial Seamount itself! And the dark purple areas around it are much deeper, down around -2500m or more.

The axes are just in grid index numbers (0–654 and 0–910) rather than actual lat/lon coordinates though — is that something we'd fix to make it a proper map?

## Exchange 24

**Teacher:** Nice read on the structure — yes, that shallow feature on the left is the Axial caldera rim. You've got the bathymetry and colorbar, which is the core.

Two things left to complete the capstone:

1. **Real coordinates on the axes** — the coordinate arrays are already in `ds` (you saw `lat` and `lon` in the output). You can pull them out just like you pulled out `elevation`. Once you have them, `plt.imshow()` has an `extent` parameter that takes `[lon_min, lon_max, lat_min, lat_max]`, and `origin='lower'` to flip the image right-side-up geographically. Can you try adding that?

2. **Plate boundary overlay** — `plate_boundary.csv` is in `~/data/`. It has `lon` and `lat` columns (you peeked at the header in Step 0). Load it with `pd.read_csv(...)` and add it to the plot with `plt.plot(...)`. **One heads-up**: that CSV spans the whole NE Pacific — if you just plot all of it, you'll get a line snaking across the entire figure. How would you keep the boundary clipped to just Axial's area?

Work through whichever piece feels less obvious first — what do you try?

_student tools: Bash(uv run python -c "
import xarray as xr
import matplotlib.pyplot as plt
import pandas as pd

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')

lats = ds['lat'].values
lons = ds['lon'].values

lon_min, lon_max = lons.min(), lons.max()
lat_min, lat_max = lats.min(), lats.max()

boundary = pd.read_csv('data/plate_boundary.csv')
clipped = boundary[(boundary['lon'] >= lon_min) & (boundary['lon'] <= lon_max) & (boundary['lat'] >= lat_min) & (boundary['lat'] <= lat_max)]

plt.imshow(ds['elevation'], extent=[lon_min, lon_max, lat_min, lat_max], origin='lower', aspect='auto')
plt.colorbar(label='elevation (m)')
plt.plot(clipped['lon'], clipped['lat'], 'r-', linewidth=1.5)
plt.xlabel('longitude')
plt.ylabel('latitude')
plt.title('Axial Seamount bathymetry')
plt.savefig('work/bathymetry_map.png', dpi=100, bbox_inches='tight')
print('saved!')
"); Read(/tmp/onramp_p2_first_year_kbs99m3a/student/work/bathymetry_map.png)_
**Student:** Oh this is so much better! Real coordinates on the axes now — I can actually see it's centered around 46°N, 130°W like we said.

And the red line — that's the plate boundary, running roughly north-south through the middle of the map. There's a little oval loop shape in it around 46°N, 130°W... is that tracing the caldera? Like the boundary has some kind of feature right where the volcano is?

The shallow yellow-green summit is on the left side of that red line. So the spreading center runs right through (or right next to) Axial — which makes total sense given the double magma supply thing we talked about earlier.

This worked first try! I wasn't sure the filtering syntax would be right but it seems like it was. Did I do the clipping the right way, or is there a cleaner pandas way to write that condition?

## Exchange 25

**Teacher:** That oval in the boundary line is the Juan de Fuca ridge trace looping around the Axial hotspot — you're seeing the spreading center + hotspot interaction in the data itself. Nice catch.

On the filtering: however you wrote it is almost certainly fine. The clean pandas idiom is `.between()`, which avoids the repetition:

```python
clipped = boundary[
    boundary['lon'].between(lon.min(), lon.max()) &
    boundary['lat'].between(lat.min(), lat.max())
]
```

If yours did the same thing with `>=` / `<=` comparisons, it's correct — just a bit more verbose.

---

**Step 1 gate cleared.** Bathymetry + colorbar + boundary overlaid + real coordinates. That's Axial on a map you built from scratch.

Quick feedback before Step 2 — 30 seconds:

1. **Difficulty:** Too easy / about right / too hard?
2. **Friction:** Anything confusing, broken, or where you got stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty:** About right — maybe even a little satisfying? The extent/origin piece was new but the hint gave me just enough to figure it out without copying code directly.

2. **Friction:** The `aspect='auto'` — I just threw that in because the image looked squished when I was testing mentally, but I'm not totally sure what it does or if it's the right choice. I'd want to understand that better. Also I wasn't 100% sure if `origin='lower'` was needed or what it was flipping, I just trusted the hint.

3. **Keep/cut:** Keep the "describe what you see" expectation — being asked to actually interpret the map rather than just produce it made me notice the oval in the boundary line, which I wouldn't have spotted otherwise. If I could change one thing, I'd want a one-sentence explanation of *why* imshow flips images by default (the row-zero-at-top thing) — knowing the reason would help me remember the fix.

Ready for Step 2!

## Exchange 26

_teacher tools: Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md)_
**Teacher:** Step 1 cleared. On to Step 2.

---

**Step 2 — Why it's there** *(Compressed — no code, just concepts)*

You already articulated the core of this during the assessment. The capstone is a slight extension — applied framing rather than a repeat:

> Connect Axial's **double magma supply** (spreading ridge + hotspot) to its **frequent eruptions** — and name **one specific thing** a cabled real-time observatory catches that a yearly ship visit would miss.

Take a shot at it in your own words.

**Student:** Okay so — Axial sits on top of both the Juan de Fuca spreading ridge *and* a hotspot plume underneath. The spreading ridge is already pulling the plates apart and letting magma up, but then you've also got this extra heat source punching up from below. So the magma supply is basically doubled — more heat, more pressure building in the magma chamber, which means it erupts more frequently than a typical mid-ocean ridge site would.

For the observatory piece — if you only visit by ship once a year, you'd see the *before* and *after* of an eruption but you'd completely miss the eruption itself, and probably the buildup too. A cabled real-time observatory would catch the actual moment of eruption — the pressure spike, the temperature jump at the vents, maybe even the seismic swarm right before it starts. That warning signal before the eruption is exactly the kind of thing that tells you *how* these systems work mechanically, not just that they erupted at some point between your visits.

Is there a specific eruption the observatory actually caught that I should know about?

## Exchange 27

**Teacher:** That's the complete answer — the mechanical "why" (double supply → pressure → frequent eruptions) and the monitoring "why" (the precursor matters as much as the event).

On your question: yes, a real one. Axial erupted in **April 2015**, and OOI had just been cabled. The network caught the seismic swarm, the seafloor deflation, and the vent temperature changes in real time — the first submarine eruption ever recorded with a full cabled array in place. That's exactly the kind of data that feeds into the tmpsf and magma2vents projects you'll be joining.

---

Step 2 gate cleared. Quick feedback:

1. **Difficulty:** Too easy / about right / too hard?
2. **Friction:** Anything confusing, broken, or where you got stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty:** Easy, but in a good way — it felt like connecting dots I'd already laid out rather than learning something new. The right level for a "compressed" step.

2. **Friction:** None really. No code, clear prompt, I knew what was being asked.

3. **Keep/cut:** Keep the closing fact — that the 2015 eruption was actually caught in real time is genuinely exciting and makes me want to dig into the data more. That's the kind of detail that makes the work feel real rather than just an exercise. Nothing to cut.

Also — knowing the data I'm about to work with is from *that specific eruption* changes how I'm thinking about it. The temperature sensors from Step 3 are from January 2015, right before it happened? That's kind of a lot.

Ready for Step 3.

## Exchange 28

_teacher tools: Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md)_
**Teacher:** ---

**Step 3 — Get the data** *(Compressed)*

Quick domain framing before the capstone: that temperature data didn't arrive by magic. OOI's Axial observatory has sensors wired to a junction box on the seafloor, which connects via fiber-optic cable to a shore station in Oregon, which feeds a cloud data archive. When you run `xr.open_dataset(...)`, you're holding the end of that cable.

You opened this exact file during the assessment. The capstone here is doing it formally, in your notebook, and reporting what you find:

> Open `data/axial_tmpsf_2015-01.nc` with xarray. Report its **dimensions**, **coordinates**, and the **temperature variable** — specifically its shape and what the two axes represent.

Go ahead — you've done this before. Let me know what you see.

_student tools: Bash(uv run python -c "
import xarray as xr
ds = xr.open_dataset('data/axial_tmpsf_2015-01.nc')
print(ds)
print()
print('temperature shape:', ds['temperature'].shape)
")_
**Student:** Okay, here's what I've got:

- **Dimensions:** `time` (359 steps) and `sensor` (24 sensors)
- **Coordinates:** `time` runs from January 1 to January 15, 2015 — about two weeks of data, a few months before the eruption. `sensor` is numbered 1–24.
- **Temperature variable:** shape is (359, 24) — so 359 time steps by 24 sensors. The first axis is time, second is which sensor you're reading. Each sensor is presumably at a slightly different spot on the hydrothermal vent field.

So this is a 2-week snapshot of 24 thermistors sitting on the seafloor at Axial, recording temperature at regular intervals. The fact that it's January 2015 — a few months before the April eruption — means we might actually see something interesting if temperatures were changing in the lead-up?

Step 3 gate — what do you need from me to clear it?

## Exchange 29

**Teacher:** That's the full picture — dims, coords, variable shape, and what the axes mean. Gate cleared.

And yes: you're holding pre-eruption baseline temperature data. Whether you can see a thermal signal building in January is a real research question. That's not the capstone here, but it's exactly the kind of thing that lives in the tmpsf project you're about to join.

Quick feedback before the last step:

1. **Difficulty:** Too easy / about right / too hard?
2. **Friction:** Anything confusing, broken, or where you got stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty:** Easy — I'd done it once already, so this was basically just running the same code in a cleaner context. Felt right for a "compressed" step.

2. **Friction:** None. The only tiny thing is I added `.shape` at the end to get the explicit dimensions tuple — I wasn't sure if xarray would show that in the main print output or if I needed to ask for it separately. Minor.

3. **Keep/cut:** Keep the domain framing at the top — "you're holding the end of that cable" actually made me think differently about what `open_dataset` is doing. It's not just loading a file, it's the last step in a chain from a physical sensor on the seafloor. That framing is worth keeping for future steps too. Nothing to cut.

Ready for Step 4 — the resample confirmation!

## Exchange 30

_teacher tools: Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md)_
**Teacher:** Step 3 cleared. Here's your confirmation moment.

---

**Step 4 — See the science** *(Capstone only)*

> Load `data/axial_botpt_2015-01.parquet` with `pd.read_parquet(...)`, resample `bottom_pressure_psi` to hourly means, plot a few days of it, and describe what you see. What pattern is in the data, and what is it?

The resample syntax you guessed in the assessment was correct — now run it for real and tell me what the plot looks like.

_student tools: Bash(uv run python -c "
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_parquet('data/axial_botpt_2015-01.parquet')
print(df.head())
print()
print('shape:', df.shape)
print('index:', df.index[:3])
"); Bash(uv run python -c "
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_parquet('data/axial_botpt_2015-01.parquet')
hourly = df['bottom_pressure_psi'].resample('1h').mean()

hourly.iloc[:72].plot(figsize=(10, 4))
plt.ylabel('bottom pressure (psi)')
plt.title('Axial Seamount — seafloor pressure, first 3 days (hourly mean)')
plt.tight_layout()
plt.savefig('work/pressure_tides.png', dpi=100)
print('saved!')
print(hourly.head(10))
"); Read(/tmp/onramp_p2_first_year_kbs99m3a/student/work/pressure_tides.png)_
**Student:** The resample worked! And I can clearly see the oscillation — pressure goes up and down roughly **twice a day**, with each cycle taking about 12 hours. That's the tidal signal, exactly what we expected.

The pressure swings between about 2252 and 2255.5 psi — so roughly a 3.5 psi range. Each high tide adds more water weight above the sensor, pushing the pressure up; low tide pulls it back down.

And you can see the two daily peaks aren't quite equal — one is a bit higher than the other each day. Is that the difference between spring and neap tides, or something about the moon's position? The ~12.4-hour period is showing up clearly rather than exactly 12, which matches what you mentioned earlier.

Step 4 gate — did the resample syntax come out right?

## Exchange 31

**Teacher:** The resample syntax was right, the plot shows the signal, and you read it correctly — amplitude, period, and even the unequal peaks.

On the peak inequality: that's not spring/neap (that's a monthly pattern when sun and moon align or oppose). What you're seeing is the **diurnal inequality** — multiple tidal constituents (the lunar M2, the solar S2, and diurnal K1/O1 components) stacking and partially canceling each other. The ~12.4 h period is M2 dominating, but the others modulate the heights. You'll dig into tidal constituents properly in the project work.

**Step 4 gate cleared. Slice complete.**

---

You've now touched every tier of the journey:

- Shell and vi to reach the data
- Coordinates and matplotlib to locate and see it
- Plate tectonics and OOI to understand why it's there
- xarray to open a real observatory dataset
- pandas to find a physical signal in it — the lunar tide pressing on the seafloor at Axial, two months before the eruption

**Bridge:** That tidal loading doesn't just press on the seafloor — it modulates the flow through hydrothermal vents. The pressure fluctuations you just plotted are the same forcing that drives the diffuse vent flow variability the tmpsf and magma2vents projects are studying. You're at their doorstep now.

---

Last feedback round:

1. **Difficulty:** Too easy / about right / too hard?
2. **Friction:** Anything confusing, broken, or where you got stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty:** About right for a capstone — it asked me to do something real and interpret it, not just reproduce something we'd already done. The resample syntax confirmation was satisfying.

2. **Friction:** None on the code side. I wasn't sure how many days to plot initially — I picked 3 because it seemed like enough to see the pattern clearly, but it was a judgment call. A small hint like "plot enough to see at least 2–3 full cycles" would remove that uncertainty.

3. **Keep/cut:** Keep the "bridge" paragraph at the end connecting the tidal signal to the actual research. That's the moment the whole session snapped into focus — I wasn't just practicing pandas, I was looking at the forcing that drives the thing the lab is actually studying. That framing should come at the end of every step, not just the last one.

And — this was genuinely more interesting than I expected. I came in thinking it would be a dry setup exercise and I ended up curious about whether there's a thermal precursor in the January temperature data before the April eruption. Is that a question I'm allowed to just go poke at, or do I wait for formal project onboarding?

## Exchange 32

_teacher tools: Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_kbs99m3a/teacher/progress.md)_
**Teacher:** All five steps cleared. Slice complete.

---

One last thing: `feedback.md` is local to your clone by default — Dr. Soule never sees it unless you share it. Your notes are detailed and specific (the bridge suggestion, the vi abstraction gap, the "plot how many days" uncertainty) — exactly the kind of thing that improves the next version of this for other students.

Would you be willing to share it with Dr. Soule? If yes, two options:

1. **Email it:** attach `feedback.md` from your clone to dax.soule@qc.cuny.edu with a quick note.
2. **GitHub issue:** open an issue on `qc-soule-lab/student-onramp` and paste the contents (or upload the file).

Either works. Totally your call.

**Student:** Yeah, definitely — happy to share it. Honest feedback only helps if it actually gets to the person who can use it.

I'll go with the GitHub issue since it seems like that's where the course materials live anyway, so it makes sense for it to end up there. I don't have the repo access set up yet though — is that something I get during formal project onboarding, or can I just open an issue without being a collaborator? I know some repos allow outside contributors to file issues.

If that doesn't work, I can email it to Dr. Soule directly — I have the address.

## Exchange 33

**Teacher:** Most public GitHub repos allow non-collaborators to file issues, but I don't know if `qc-soule-lab/student-onramp` has issue creation restricted — you'd find out quickly enough by trying. If it blocks you, email is the sure path and you have the address.

Either way, consent is noted and you have both options. The feedback is genuinely useful — the bridge-at-every-step suggestion especially.

That's the full onramp. You came in with no terminal experience and left with a plot of a pre-eruption tidal signal from a cabled seafloor observatory. Good session, Riley. Welcome to the lab.

---

# Generated artifacts (archived from the teacher workspace)

## journey_plan.md

```markdown
*AI-generated draft (Claude, Anthropic) — for review. This plan was generated from your assessment; all leveling follows the version-controlled rubric.*

# My journey plan — "From the map to the tidal signal"

**Student:** Riley  ·  **Generated:** 2026-06-10

<!-- PRIVATE: this file is gitignored and stays in your clone. -->

## How you were placed
Each step below shows the **level** the assessment found and the **treatment** that follows from it (see `assessment/rubric.md`). You can see exactly why each step is set the way it is.

| Step | Domain question | Your level | Treatment | Book chapter |
|---|---|---|---|---|
| 0. Get to the data | How do you reach the data on the machine? | Developing | Compressed | [Unix basics](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/environment/intro_to_unix.md) · vi: [Wikibooks vi reference](https://en.wikibooks.org/wiki/Learning_the_vi_Editor/vi_Reference) |
| 1. Locate it | Where is Axial & what's its seafloor? | Developing | Compressed | [JupyterLab](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/environment/intro_to_jupyterlab.md) · [Python fundamentals](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/core_python/python_fundamentals.ipynb) · [NumPy & matplotlib](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/basic_scipy/numpy_and_matplotlib.ipynb) |
| 2. Why it's there | Why a volcano + why OOI? | Developing | Compressed | — (conceptual) |
| 3. Get the data | How does OOI data reach you? | Developing | Compressed | [xarray intro](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/xarray/xarray_intro.md) |
| 4. See the science | Can you see the seafloor tide? | **Proficient** | **Capstone only** | [pandas intro](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/pandas/pandas_intro.md) · [pandas basics](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/pandas/basic_pandas.ipynb) |

**Compressed** means: skip the chapter reading; go straight to the exercise with hints available on demand; then the capstone.
**Capstone only** (step 4) means: you already showed the concept — go straight to confirming it in code.

## What you'll do

**Step 0 — Get to the data**
From the JupyterLab terminal: cd to the data dir, ls the staged grid, and open `plate_boundary.csv` in vi to read its header — then exit cleanly.

**Step 1 — Locate it**
Plot Axial's location + bathymetry from the staged grid, plate boundary overlaid. (Heads-up: `plate_boundary.csv` spans the whole NE Pacific — limit your axes to the grid's extent.)

**Step 2 — Why it's there**
Connect Axial's double magma supply (spreading ridge + hotspot) to its frequent eruptions — and name one thing a cabled real-time observatory catches that a yearly ship visit would miss.

**Step 3 — Get the data**
Open `data/axial_tmpsf_2015-01.nc` with xarray; report its dims (time, sensor), coords, and the temperature variable.

**Step 4 — See the science**
Load `data/axial_botpt_2015-01.parquet`, resample `bottom_pressure_psi` to hourly, and reveal the M2 (~12.4 h) tidal signal; describe it.

## End state
You will have touched domain concepts from tiers 0a/0b/0c AND used Jupyter, Python, matplotlib, xarray, and pandas — each in service of a real question — landing at the doorstep of the thermal/diffuse-flow projects (tmpsf, magma2vents).

**Bridge:** Tidal loading also modulates diffuse vent flow — the very question the tmpsf / magma2vents projects pursue. You're now at their doorstep.

```

## progress.md

```markdown
# My progress — "From the map to the tidal signal"

**Student:** Riley  ·  **Updated:** 2026-06-10

<!-- PRIVATE: gitignored, stays in your clone. On "continue", resume at the first step not Done. -->

| Step | Gate | Status | Level | Treatment | Capstone | Feedback |
|---|---|---|---|---|---|---|
| 0. Get to the data | cleared | done | Developing | Compressed | y | y |
| 1. Locate it | cleared | done | Developing | Compressed | y | y |
| 2. Why it's there | cleared | done | Developing | Compressed | y | y |
| 3. Get the data | cleared | done | Developing | Compressed | y | y |
| 4. See the science | cleared | done | Proficient | Capstone only | y | y |

**Gate legend:** `locked` = previous module not cleared · `unlocked` = open, capstone not yet cleared · `cleared` = gate passed, next module open.
**Next step:** slice complete — doorstep of tmpsf / magma2vents

```

## feedback.md

```markdown
# Activity feedback — Riley

<!-- LOCAL + PRIVATE by default (gitignored). Shared with Dr. Soule only with the student's consent.
     One entry appended per activity, at the END of the file (keep chronological order).
     This is how the onramp improves between students. -->

---
## Step 0 — Get to the data   (2026-06-10)

- **Difficulty:** About right, slightly easy on shell commands — possibly due to hint scaffolding rather than actual ease; coordinate/trig question landed at the right level.
- **Friction / confusing / broke:** Nothing broken. vi narration felt abstract without actually running it; internalizing keystrokes without executing them is harder.
- **Helped most / would change:** Helped: "guess from task description" habit (e.g. `head` = top of file) — reasoning from the command name feels transferable. Would cut: confirmation step after each shell command during assessment felt redundant once the command was understood.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 1 — Locate it   (2026-06-10)

- **Difficulty:** About right, satisfying — the extent/origin hint gave enough to work it out without copying code directly.
- **Friction / confusing / broke:** `aspect='auto'` added without fully understanding it (worked but reasoning uncertain); `origin='lower'` trusted from hint rather than understood. Wants one-sentence reason for imshow's default flip (row-zero-at-top screen convention vs. lat-zero-at-bottom geography).
- **Helped most / would change:** Helped: "describe what you see" expectation — led to noticing the oval in the boundary line, which pure code-production wouldn't have surfaced. Would add: brief why-explanation for imshow flip at the moment of using it.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 2 — Why it's there   (2026-06-10)

- **Difficulty:** Easy but appropriate for Compressed — connecting dots already laid out, not new learning.
- **Friction / confusing / broke:** None. No code, clear prompt, concepts were accessible.
- **Helped most / would change:** Helped: closing 2015 eruption fact made the work feel real and raised engagement for Step 3. Nothing to cut. Note: student made unprompted connection that Jan 2015 TMPSF data is pre-eruption baseline — strong scientific intuition.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 3 — Get the data   (2026-06-10)

- **Difficulty:** Easy — had done it once in the assessment; re-running in a clean notebook context felt appropriate for Compressed.
- **Friction / confusing / broke:** Minor: needed `.shape` to get explicit dimensions tuple; uncertain if xarray's default repr would show it. No errors.
- **Helped most / would change:** Helped: "end of that cable" domain framing reframed what `open_dataset` means — last step of a physical sensor chain, not just file loading. Worth carrying to future steps. Nothing to cut.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 4 — See the science   (2026-06-10)

- **Difficulty:** About right — real task, real interpretation, satisfying resample confirmation.
- **Friction / confusing / broke:** No code friction. Judgment call on how many days to plot (chose 3); would benefit from a small prompt like "plot enough to see 2–3 full tidal cycles."
- **Helped most / would change:** Keep the bridge paragraph — moment the whole session connected (not just pandas practice, but the actual tidal forcing the lab studies). Suggestion: add a brief bridge note at the END of every step, not just the last one, so the domain connection is live throughout. Nothing to cut.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** capstone_only   ·   **Level:** proficient
---

<!-- ⟂ END MARKER: keep this as the LAST line. Insert each new entry IMMEDIATELY ABOVE
     this marker (it is the one unique anchor — entries then stay chronological). -->

```
