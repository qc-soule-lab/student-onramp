*AI-generated transcript (Claude, Anthropic) — synthetic persona run for curriculum acceptance; no real student. See `validation/persona_acceptance_plan.md`.*

# Persona run — p1_hs_senior

**Started:** 2026-06-10T22:46:31+00:00 · **Status:** completed · **Exchanges:** 44
**Teacher:** `claude-sonnet-4-6` (tools: Read,Glob,Grep,Write,Edit) · **Student:** `claude-sonnet-4-6` (tools: Bash,Read,Write,Edit,Glob)
**Privacy check (in teacher workspace):** `journey_plan.md` ✓ gitignored, `progress.md` ✓ gitignored, `feedback.md` ✓ gitignored

## Exchange 1

_teacher tools: Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/journey/journey.yml); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/skill_maps/slice_v1.yml); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/rubric.md); Glob({"pattern": "assessment/probes/*.md", "path": "/tmp/onramp_p1_hs_senior_65d3ipsq/teacher"}); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p00_unix_navigate.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p00c_shell_peek.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p00d_read_error.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p00b_vi_edit.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p01_meters_per_degree.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p02_matplotlib_bathy.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p03_why_axial.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p04_xarray_inspect.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/assessment/probes/p05_pandas_resample.md)_
**Teacher:** Good — I have everything I need. Let me welcome Sam properly and kick off the assessment.

---

Hi Sam! Welcome to the lab, and welcome to the summer internship.

Before we get into the science, I want to run a short assessment. This isn't a test you can pass or fail — it's just so we can start you at the right level and not waste your time explaining things you already know (or flying past things you haven't seen yet). Be honest; there are no wrong answers, only useful ones.

It covers the shell, a little geography, some Python plotting, and a few tools we use for oceanographic data. Shouldn't take long.

Ready? Let's start with Step 0 — the shell.

---

**Question 1 of 4 (shell basics):**

The staged bathymetry grid lives in `~/data/`. From the JupyterLab **terminal**, show the commands you'd use to:
1. Move into that directory
2. Confirm the file `axial_bathymetry_gmrt.nc` is there
3. See how big it is

**Student:** um... okay so i've never actually opened a terminal before? like i know it's a thing but i don't really know what commands are or how to type in them.

is a "directory" like a folder? and what does `~/data/` mean — is the `~` like a shortcut for something?

## Exchange 2

**Teacher:** Great questions — those are exactly the right things to wonder about.

Yes: a **directory is just a folder**. Same thing, two names. And yes: **`~` is a shortcut for your home directory** — the folder the system drops you into when you first log in. So `~/data/` means "the `data` folder inside your home folder."

Since you haven't used a terminal before, let me give you a nudge rather than just asking you to guess from nothing.

---

**Hint (R1):** The terminal has two jobs here: *move you into a folder* and *show you what's inside it*. There are two classic short commands for those two jobs. One starts with the letter `c`, one starts with `l`. Do those ring any bells — maybe from something you've seen in a tutorial, a class, or even just online?

(No worries if not — I'll walk you through it either way. Just take a guess if anything comes to mind.)

**Student:** oh um, maybe... "change" for the c one? like change to the folder? i don't know the exact thing to type though.

and for the l one maybe "list"? like list what's in it?

i really am just guessing here lol, sorry

## Exchange 3

**Teacher:** Don't apologize — that's **exactly right.** Your instincts are good. The commands are literally just the abbreviations:

- **`cd`** = "change directory" → moves you into a folder
- **`ls`** = "list" → shows what's inside

So the sequence for our three goals would be:

```
cd ~/data
ls -lh
```

The `-lh` part is a couple of flags tacked onto `ls`: `-l` gives you a detailed list (one file per line with permissions, size, etc.), and `-h` makes sizes **h**uman-readable — so you see `45M` instead of `47185920`.

---

Now that you've seen those: looking at the original question, the three goals were (1) move into `~/data/`, (2) confirm `axial_bathymetry_gmrt.nc` is there, and (3) see how big it is.

Can you tell me, in your own words, which command does each job? (You've got all the pieces now — just connect them.)

**Student:** oh okay that actually makes sense! so like...

`cd ~/data` moves me into the folder, and then `ls -lh` shows me what's in it — including how big the files are?

so both the "is it there" and "how big is it" questions get answered by the `ls -lh` part?

## Exchange 4

**Teacher:** Exactly right! `ls -lh` does both at once — you see every filename in the folder (so you know it's there) and the size next to it. Nice catch.

Okay, moving to the next one.

---

**Question 2 of 4 (shell):**

Before you load a file in Python, you want a quick look at the **top** of `plate_boundary.csv` — just the column header and the first couple of rows — straight from the terminal, **without opening an editor**. What command would you use?

**Student:** um... i don't know this one. i'm guessing maybe something like `show` or `read`? 

like `read plate_boundary.csv`? that sounds way too simple but idk what else it would be

## Exchange 5

**Teacher:** Good instinct to guess — `read` isn't it, but the real command is just as short.

**Hint (R1):** There's a command that **prints a file's contents straight to the screen** — like, dumps the whole thing. And there's a second command that does the same thing but **only the first few lines**. Do either of those ring a bell? (Think of something cat-like, or something that describes the top of something.)

**Student:** oh wait, "the top of something"... is it `head`? like the head of the file?

and the cat one — is that literally just `cat`?? that seems too funny to be real lol

## Exchange 6

**Teacher:** Yes and yes — `head` and `cat` are both real, and `cat` really is just called `cat` (short for "concatenate," though everyone just uses it to dump files).

The important distinction:
- **`cat`** prints the *whole* file — fine for a tiny CSV, but if it's a million rows it floods your terminal
- **`head`** prints just the top ~10 lines — the right move when you just want to peek at the header

You had it exactly right. `head plate_boundary.csv` is the clean answer here.

---

**Question 3 of 4 (shell):**

You run `cat plate_boundary.csv` and the terminal prints:

```
cat: plate_boundary.csv: No such file or directory
```

You know the file exists somewhere in the project. **What does this error mean, and what's your next move?**

**Student:** oh, i think it means i'm in the wrong folder? like the file exists somewhere but i'm not currently *in* the folder where it lives?

so maybe i'd do `ls` to see what's in my current folder, and then `cd` into the right one? like look around until i find where `plate_boundary.csv` actually is?

## Exchange 7

**Teacher:** Exactly right, no hints needed — and that's the key insight: the error almost never means the file is gone or broken. It just means the shell is looking in the *wrong place*. You'd use `pwd` to confirm where you are, `ls` to see what's there, then `cd` to the right folder (or just give `cat` the full path directly).

---

Last step-0 question, different topic:

**Question 4 of 4 (vi):**

From the terminal, open `plate_boundary.csv` in **vi**, read its header line, then leave the editor **without changing the file**. Walk me through the keystrokes — including how you'd quit normally, and how you'd quit if you'd accidentally typed something.

**Student:** um. i have no idea what vi is? like i've never heard of it.

i could probably type `vi plate_boundary.csv` to open it based on how the other commands worked... but i genuinely don't know what i'd do once i was in there. like how do you even type in it or leave it?

## Exchange 8

**Teacher:** Good start — `vi plate_boundary.csv` is exactly right for opening it. You figured that out by analogy, which is solid.

**Hint (R1):** vi has two "modes" — one where keypresses are **commands** (move, quit, etc.), and one where keypresses are **text you're typing**. When you first open a file, which mode do you think it starts in? And what key might get you back to "command mode" if you accidentally started typing?

(Think: is there a key on the keyboard that means "escape" or "cancel"?)

**Student:** oh, maybe it starts in command mode? like it would be weird if it just let you start typing immediately when you open a file to read it.

and yeah escape seems obvious lol — that's like the "get out of whatever i'm doing" key on everything

## Exchange 9

**Teacher:** Both exactly right. vi does start in normal/command mode — and `Esc` is indeed the "get back to safety" key.

The tricky part is **how you quit**, because it's not obvious. Here's the full picture:

```
vi plate_boundary.csv   # open the file — lands you in normal mode
                        # (read the header line)
Esc                     # make sure you're back in normal mode
:q                      # quit (file unchanged)
:q!                     # quit and DISCARD any accidental edits
:wq                     # save AND quit (for when you want to keep changes)
```

The colon (`:`) tells vi "I'm about to type a command," then `q` = quit, `!` = force/discard, `w` = write/save. The **bail reflex** is `Esc` then `:q!` — gets you out no matter what.

---

With that in front of you: can you walk me through what you'd type to open the file, read its header, and leave **without saving** — including what you'd do if you'd accidentally pressed `i` and started typing something?

**Student:** okay so... `vi plate_boundary.csv` to open it, then just look at the top line on the screen.

then if i hadn't pressed anything i'd do `Esc` then `:q` to quit?

and if i accidentally hit `i` and started typing stuff, i'd hit `Esc` first to get back to command mode, then `:q!` to bail out without saving?

## Exchange 10

**Teacher:** That's exactly it — you've got the full mental model: normal mode → read → `Esc` → `:q` clean, or `Esc` → `:q!` if you need to bail. That's the survival skill.

---

Step 0 done. Now a bit of geography — no terminal needed.

**Axial Seamount is near 46°N, 130°W.**

(a) Roughly how many **kilometers** is 1° of latitude?

(b) At 46°N, is 1° of **longitude** longer, shorter, or the same distance in km as 1° of latitude — and why?

**Student:** um okay i really don't know this one. like... latitude is the up-down lines? or the across ones? i always mix those up.

and i definitely don't know how many km a degree is. like i know degrees exist on maps but i've never thought about what that means in actual distance.

can you just tell me the answer? i won't remember this anyway, it feels like the kind of thing you'd just google

## Exchange 11

**Teacher:** Ha, fair point — you would google it! And that's fine for a fact. But the reason we're doing this isn't to memorize 111 km/degree; it's so that when you're staring at a map grid labeled `-130.5` to `-129.8`, you have gut-feel for whether that's a few kilometers or a thousand. That intuition helps a lot when something looks weird in the data.

Also: latitude is the **across** lines (east-west stripes) — they measure how far **north or south** you are. Longitude is the **up-down** lines. Lat = "ladder rungs," lon = "longitude runs left-right." (Now you can forget the mnemonic and just know it.)

I'm not going to skip to the answer, but I'll walk you there. One step:

**Hint (R1):** Earth's circumference is about **40,000 km**. A full circle is **360°**. If you divide those two numbers, what do you get — and what does that tell you about 1° of latitude?

**Student:** oh okay so 40,000 divided by 360... that's like... um, a little over 100? 

is it around 111 km per degree?

## Exchange 12

**Teacher:** Exactly — 111 km per degree of latitude. You did the arithmetic yourself from one fact.

Now part (b). Think about a **globe** (not a flat map — a real sphere). Latitude lines run parallel across it, always the same distance apart. But longitude lines run from pole to pole.

What happens to those longitude lines as they get close to the poles — do they stay the same distance apart, or do they squeeze together?

**Student:** oh, they squeeze together right? like on a globe they all meet at the top (the poles) so they have to be getting closer together as you go north

so at 46°N, 1° of longitude would be *shorter* than 1° of latitude?

## Exchange 13

**Teacher:** Exactly right on both counts. The technical detail: the shrinkage follows `cos(latitude)`, so at 46°N, 1° of longitude ≈ 111 × cos(46°) ≈ **77 km** — about 30% shorter than a degree of latitude. Useful to have in your head when you're zooming a map.

---

Next one — coding this time.

**You've loaded a 2D NumPy array called `depth` (rows = latitude, columns = longitude) of seafloor depths. Write the matplotlib code to display it as an image with a colorbar, and say how you'd make clear what the colors mean.**

**Student:** i've never written any code before so i genuinely have no idea here. like i don't know what matplotlib is or how you even write python at all.

is this something you're going to teach me or am i supposed to already know it?

## Exchange 14

**Teacher:** Great question to ask — yes, this is absolutely something we'll teach. The assessment just figures out *how much* of a running start you need, not whether you need it at all.

Since you've never coded before, let me give you one nudge and see if you can reason through it even without knowing the exact words:

**Hint (R1):** Matplotlib is a Python library for making plots and images. If you had a grid of numbers and wanted to show it as a picture — like turning depth values into colors — what do you think the command might look like? And if you wanted a little scale bar on the side showing what each color means, what might that be called?

(Even a totally made-up guess is fine — I'm curious how you reason about it.)

**Student:** um... maybe something like `plot(depth)` to show it? that seems too simple but that's my guess.

and for the scale bar thing — like a key that shows what each color means? i'd call it a "legend" or a "color key"? i don't know what the actual word is in code.

## Exchange 15

**Teacher:** Good reasoning — `plot` exists, but it's for line charts. For a 2D grid of numbers displayed as a color image, matplotlib has a specific function. And you're one letter away on the scale bar — it's called a **colorbar** (not legend; legends are for line plots).

Here's the real code:

```python
import matplotlib.pyplot as plt
plt.imshow(depth)
plt.colorbar(label="depth (m)")
plt.show()
```

`imshow` = "image show" — turns a 2D array into a colored image. `colorbar()` adds the scale on the side; the `label=` tells a reader what the colors actually mean (depth in meters, not just a rainbow).

---

With that in front of you: what are the three things you need for this to be a complete, readable plot — and why does the label on the colorbar matter?

**Student:** okay so the three things are `imshow(depth)` to actually show the grid as colors, `colorbar()` to put the scale on the side, and `show()` to make it actually appear on screen?

and the label matters because without it you just have like... a rainbow? you wouldn't know if the colors meant depth in meters or temperature or whatever. the label is what tells someone "oh blue means 2000 meters deep" or whatever.

## Exchange 16

**Teacher:** Exactly — without the label it's just a pretty rainbow with no meaning. You nailed all three components and got the *why* exactly right.

---

Almost done. This one's just words, no code:

**In 2–3 sentences: why is there a volcano at Axial Seamount, and why did scientists build a cabled underwater observatory there?**

(If you've never heard of Axial Seamount or the Juan de Fuca ridge, that's totally fine — just say what you know or think, and we'll go from there.)

**Student:** i've never heard of axial seamount before, but i'm guessing the volcano is there because of the plates moving? like where two plates pull apart or push together, magma comes up through the crack? i remember that from earth science.

but i have no idea why scientists built an observatory there specifically. is it like... because it's active and interesting to watch? or because it's hard to study otherwise since it's underwater?

## Exchange 17

**Teacher:** You've got both halves, which is the important thing. Let me sharpen them:

**The geology:** You're right — it's a spreading ridge (plates pulling *apart*), so magma wells up. That's Axial's main engine. It also sits over a persistent hotspot-like magma source on top of that, which is why it's the most volcanically active site in the northeast Pacific — it actually erupted in 2011 and again in 2015.

**The OOI rationale:** Your guesses were both correct — it erupts every few years, *and* it's underwater, so without a permanent cabled connection to shore you'd only catch an eruption if a ship happened to be nearby. The cable gives continuous, real-time data on a live submarine volcano. That's why it's wired.

Good reasoning from limited info.

---

Two more — these are coding, similar to the matplotlib one.

**You have a NetCDF file `data/axial_tmpsf_2015-01.nc` — a dataset of seafloor temperature measurements. Using xarray, how do you open it and find out what variables, dimensions, and coordinates it contains?**

**Student:** i don't know xarray, but based on what i just learned — like how you do `import matplotlib.pyplot`... maybe there's an `import xarray` and then some kind of `open()` command?

like `xarray.open("data/axial_tmpsf_2015-01.nc")`? and then maybe just printing it shows you what's inside?

## Exchange 18

**Teacher:** Good pattern transfer — `import xarray` is exactly right, and yes, displaying the dataset object shows you its structure. You're one word away on the function name.

**Hint (R1):** The xarray function to open a file isn't just `open()` — it's more specific, because you're opening a whole *dataset* (which can have many variables and dimensions). What do you think the full function name might be?

**Student:** oh... `open_dataset`?

## Exchange 19

**Teacher:** Exactly — `xr.open_dataset("path/to/file.nc")` returns a Dataset object, and just displaying it (typing `ds` in a notebook cell) gives you a rich summary of its dimensions, coordinates, and variables. You can also use `ds.dims`, `ds.coords`, `ds.data_vars` to inspect each part directly.

Good reasoning from the pattern.

---

Last one:

**You load `data/axial_botpt_2015-01.parquet` like this:**

```python
df = pd.read_parquet("data/axial_botpt_2015-01.parquet")
```

**It has a datetime index and a column `bottom_pressure_psi` with 1-minute samples.**

(a) How do you compute an **hourly mean** using pandas `resample`?

(b) The data has a **~12.4-hour oscillation** in it. How would you make it visible — and what do you think that signal might be?

**Student:** okay for part (a) i'm guessing maybe `df.resample("hour").mean()`? like tell it to group by hour and take the average? i don't know the exact syntax but that feels like the shape of it.

for part (b) — 12.4 hours is almost exactly half a day... is that tides?? like i know tides go in and out twice a day from watching ocean stuff, so that would be like once every 12 hours? the .4 is a bit off but that seems close. you'd probably just plot it to see the waves?

## Exchange 20

**Teacher:** The tidal reasoning is excellent — that's genuine intuition, and you're right. It's the **M2 lunar semidiurnal tide**: the moon pulls the ocean in a ~12.42-hour cycle, and it reaches all the way to the seafloor. Very few people arrive here already thinking about that.

For part (a), you've got the right shape — `resample().mean()` is correct. But pandas uses specific frequency strings, not words like `"hour"`.

**Hint (R1):** What do you think the string for "hourly" might be — like a short abbreviation? And as a bonus: if you resampled all the way to *daily* instead of hourly, what would happen to a 12-hour oscillation in your data?

**Student:** maybe just `"H"` for hourly? like a capital H?

and for the daily thing... if you averaged over a whole day, you'd lose the 12-hour wave right? like if it goes up and down twice a day, averaging the whole day would just flatten it out.

## Exchange 21

_teacher tools: Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/templates/journey_plan.template.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/templates/progress.template.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/content/skill_to_chapter.yml); Bash(ls /tmp/onramp_p1_hs_senior_65d3ipsq/); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/.gitignore); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/journey/readings.md); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/templates/feedback.template.md); Write(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/journey_plan.md); Write(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md); Write(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/feedback.md)_
**Teacher:** Here's your plan, Sam. Take a look and let me know if anything seems off:

---

**Assessment summary:** You came in as **Developing** across all five steps. That means the **Compressed** path throughout — you skip the pre-reading and go straight into the exercises, with hints available whenever you get stuck.

A few things worth noting:

- You had zero shell experience but figured out `cd` and `ls` by reasoning ("change" and "list") — that's a good sign.
- You diagnosed the "No such file or directory" error cold, with no hints — that's actually the hardest one to get right intuitively.
- Your tidal intuition on the last question ("twice a day → 12 hours → tides?") was genuinely sharp. Most people don't make that connection on the spot.
- The areas with less background: vi, matplotlib specifics, and the Axial Seamount geology — but those are all things we're about to cover.

| Step | What you're doing | Level | Treatment |
|---|---|---|---|
| 0. Get to the data | Shell + vi capstone | Developing | Compressed |
| 1. Locate it | Plot Axial's bathymetry | Developing | Compressed |
| 2. Why it's there | Explain the volcano + OOI | Developing | Compressed |
| 3. Get the data | Open a dataset with xarray | Developing | Compressed |
| 4. See the science | Find the tidal signal | Developing | Compressed |

Does this look right to you — or is there anything you'd push back on before we start Step 0?

**Student:** honestly i'm surprised you said i did okay? i felt like i was guessing through most of it lol

"compressed" means like... faster? or just no extra reading beforehand? either way that sounds fine, i'm ready to start whenever

## Exchange 22

**Teacher:** The guessing was good guessing — you reasoned from first principles on almost everything. That's the skill.

"Compressed" just means no pre-reading: we go straight to doing it, and I'll fill in gaps as they come up rather than front-loading theory.

---

**Step 0: Get to the data**

Open a **JupyterLab terminal** (in JupyterLab: File → New → Terminal, or the Terminal tile on the Launcher). You should get a plain command prompt.

Once you've got it open, type `pwd` and hit Enter. What does it print?

_student tools: Bash(pwd)_
**Student:** it printed `/tmp/onramp_p1_hs_senior_65d3ipsq/student`

is that my home folder?

## Exchange 23

**Teacher:** Close — `pwd` shows where you *are right now*, which isn't always the same as home. Your home directory is wherever `~` points (on JupyterHub, usually `/home/jovyan`).

But you don't need to go home first. Try `ls` right where you are — what files and folders do you see?

_student tools: Bash(ls)_
**Student:** it shows `data`, `pyproject.toml`, `uv.lock`, and `work`

i see the `data` folder! so i'm already kind of in the right place?

## Exchange 24

**Teacher:** Exactly right — the data folder is right here. Now move into it and see what's inside, including file sizes. You've got the commands from earlier.

_student tools: Bash(cd /tmp/onramp_p1_hs_senior_65d3ipsq/student/data && ls -lh)_
**Student:** okay i can see all the files! `axial_bathymetry_gmrt.nc` is 1.4M, `plate_boundary.csv` is 6.3K...

and `axial_bathymetry_gmrt.nc` is definitely there like you mentioned earlier. did i do that right?

## Exchange 25

**Teacher:** You did that perfectly. `cd data` → `ls -lh` → found both files and their sizes. That's exactly right.

Before opening vi, try one more command: `head plate_boundary.csv`. It'll show you the first few lines — the column header — without opening an editor. What does it print?

_student tools: Bash(head /tmp/onramp_p1_hs_senior_65d3ipsq/student/data/plate_boundary.csv)_
**Student:** it printed a header row that says `lon,lat,segment_id` and then a bunch of coordinate numbers! 

so it's like... a list of points on a map? with longitude, latitude, and some kind of segment number?

## Exchange 26

**Teacher:** Exactly right — it's a list of GPS points tracing the plate boundary across the NE Pacific. The `segment_id` groups the points into separate line segments. That's what you'll overlay on the map in Step 1.

Now the vi part. Type:

```
vi plate_boundary.csv
```

You should see the file open. Tell me what you see on screen — and then exit without saving (you know the keystrokes from earlier).

**Student:** okay so i typed `vi plate_boundary.csv` and the file opened — i can see all the coordinates and stuff on screen.

but then i accidentally hit some key and now... it looks like i'm in that "typing" mode? like i pressed something while trying to look at the file and now there's `-- INSERT --` at the bottom of the screen.

i tried pressing `:q` but instead of quitting it just typed `:q` into the file. and i tried pressing escape but now i don't know if i'm back in the right mode or not, and i'm scared to type anything else because what if i mess up the file more.

um. i think i broke it. it won't let me out.

## Exchange 27

**Teacher:** You haven't broken anything — this is the classic vi moment and it's completely recoverable. This is exactly why `:q!` exists.

Here's your escape sequence, exactly:

1. Press **`Esc`** once (even if you already did — it's safe to press it twice)
2. Type **`:q!`** (colon, then q, then !)
3. Hit **Enter**

The `!` means "discard everything and get me out." The file is fine — vi doesn't save until you say `:wq`.

Try it and tell me what happens.

**Student:** okay i pressed Esc, then typed `:q!`, then hit Enter and...

it worked! i'm back at the regular terminal prompt! the file is still there in the listing.

oh that's actually such a relief lol, i thought i'd messed up the whole file

## Exchange 28

**Teacher:** The file is completely untouched — vi never changes anything until you explicitly say `:wq`. The `!` in `:q!` is literally there for exactly what just happened to you.

And you just completed the Step 0 capstone:
- ✓ `cd` into the data directory
- ✓ `ls -lh` — found `axial_bathymetry_gmrt.nc` and confirmed its size
- ✓ `head` — peeked at the CSV header without an editor
- ✓ `vi plate_boundary.csv` — opened it, read the header, escaped cleanly

**Step 0 cleared. Step 1 is now unlocked.**

---

Before we move on — three quick questions, 30 seconds:

1. **Difficulty:** Too easy, about right, or too hard?
2. **Friction:** Anything confusing or where you got genuinely stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change about this step?

**Student:** 1. about right i think? the beginning was kinda scary because i had no idea what any of it meant, but by the end it felt like it was making sense

2. the vi thing honestly — even knowing the commands from when you explained them, when it actually happened i just froze and forgot everything. like theory vs actually doing it is different

3. the hints really helped, especially when you explained *why* (like why `:q!` is safe). if you'd just told me the command i probably wouldn't have remembered it. i'd cut... idk, maybe the assessment felt pretty long? but i get why you needed to know where i was starting from

## Exchange 29

_teacher tools: Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/feedback.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md)_
**Teacher:** ---

**Step 1: Locate it**

The domain question is: *Where on Earth is Axial Seamount, and what does its seafloor look like?*

You're going to answer that by writing Python code to plot it. The end goal — the capstone — is a map of Axial's bathymetry with the plate boundary overlaid on top.

To do that you'll need three tools: **JupyterLab** (where you run Python), a bit of **Python**, and **matplotlib** (the plotting library you saw in the assessment). Since you're on the compressed path, we're skipping the pre-reading and jumping straight in.

Open a **new Jupyter notebook** in JupyterLab (File → New → Notebook). Once it's open, in the first cell, type this and run it (Shift+Enter):

```python
import xarray as xr
import numpy as np
import matplotlib.pyplot as plt

ds = xr.open_dataset("../data/axial_bathymetry_gmrt.nc")
ds
```

What does it print?

_student tools: Bash(uv run python -c "
import xarray as xr
import numpy as np
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
print(ds)
")_
**Student:** it printed a bunch of info! it says:

- dimensions are `lat: 654` and `lon: 910` 
- there's a coordinate for lat going from like 45.7 to 46.2 and lon from -130.5 to -129.5
- one data variable called `elevation`
- and some attributes about where the data came from

so it's like a grid — 654 rows of latitude and 910 columns of longitude, and each point has an elevation value?

## Exchange 30

**Teacher:** Exactly right — a 654×910 grid where each cell holds one elevation value. You just described a raster dataset perfectly.

Quick question before we plot: the variable is called `elevation`, and Axial Seamount is **underwater**. What do you expect those numbers to look like — positive, negative, or mixed?

**Student:** oh um... negative? because it's underwater, so it's below sea level, and i'd guess elevation below sea level is like a negative number?

but wait — it's called a *seamount* so it's a mountain sticking up from the seafloor. so maybe most of the numbers are pretty negative (like deep ocean) but the top of the seamount is less negative? like negative but closer to zero than the surrounding area?

## Exchange 31

**Teacher:** Exactly that — the seamount rises from a surrounding abyssal plain, so you'll see a cluster of less-negative values surrounded by more-negative ones. That's the topography.

Now let's look at it. Add a new cell and run this:

```python
elev = ds["elevation"].values
lat  = ds["lat"].values
lon  = ds["lon"].values

plt.figure(figsize=(8, 6))
plt.imshow(elev, origin="lower",
           extent=[lon.min(), lon.max(), lat.min(), lat.max()],
           aspect="auto")
plt.colorbar(label="elevation (m)")
plt.xlabel("longitude")
plt.ylabel("latitude")
plt.title("Axial Seamount — bathymetry")
plt.show()
```

Two things to notice once it renders: what do the colors look like, and can you spot where the seamount is? Tell me what you see.

_student tools: Bash(uv run python -c "
import xarray as xr
import numpy as np
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
elev = ds['elevation'].values
lat  = ds['lat'].values
lon  = ds['lon'].values

plt.figure(figsize=(8, 6))
plt.imshow(elev, origin='lower',
           extent=[lon.min(), lon.max(), lat.min(), lat.max()],
           aspect='auto')
plt.colorbar(label='elevation (m)')
plt.xlabel('longitude')
plt.ylabel('latitude')
plt.title('Axial Seamount — bathymetry')
plt.savefig('work/bathymetry.png')
print('saved')
"); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/student/work/bathymetry.png)_
**Student:** okay i saved it and opened it! so...

there's a big bright yellow-green area in the upper left — that must be the seamount? because the colorbar shows yellow is around -500m which is less negative than the rest. and the surrounding area is mostly dark purple-blue which is like -2000 to -2500m deep.

so the mountain is definitely sticking up way above the surrounding seafloor!

## Exchange 32

**Teacher:** You've read the map perfectly. The caldera — the collapsed summit crater — is in that yellow-green patch, and the walls drop sharply into the surrounding basin.

Now for the boundary overlay. You saw earlier that `plate_boundary.csv` has `lon` and `lat` columns. There's a library called **pandas** that loads CSV files in one line — and you'll use it a lot in Step 4. For now, just one function: `pd.read_csv(...)`.

Given what you know about how `xr.open_dataset` worked, what do you think the pandas equivalent for loading a CSV might look like?

**Student:** oh, based on how xarray worked — like `import pandas as pd` and then `pd.read_csv("../data/plate_boundary.csv")`?

you kind of already told me the function name so that felt a bit like cheating lol

## Exchange 33

**Teacher:** Ha — fair, I handed you that one. But you got the import pattern right on your own, which is the part that transfers.

Here's the full overlay. Put this in a new cell — it builds the complete capstone plot:

```python
import pandas as pd

boundary = pd.read_csv("../data/plate_boundary.csv")

plt.figure(figsize=(8, 6))
plt.imshow(elev, origin="lower",
           extent=[lon.min(), lon.max(), lat.min(), lat.max()],
           aspect="auto")
plt.colorbar(label="elevation (m)")
plt.plot(boundary["lon"], boundary["lat"], "r-", linewidth=0.5)
plt.xlim(lon.min(), lon.max())
plt.ylim(lat.min(), lat.max())
plt.xlabel("longitude")
plt.ylabel("latitude")
plt.title("Axial Seamount — bathymetry + plate boundary")
plt.show()
```

The two new lines — `plt.xlim(...)` and `plt.ylim(...)` — are why the capstone note warned you: without them, matplotlib stretches the axes to show the entire NE Pacific boundary and the seamount becomes a tiny speck.

Run it and tell me what you see — specifically, where does the boundary line fall relative to the seamount?

_student tools: Bash(uv run python -c "
import xarray as xr
import numpy as np
import matplotlib.pyplot as plt
import pandas as pd

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
elev = ds['elevation'].values
lat  = ds['lat'].values
lon  = ds['lon'].values

boundary = pd.read_csv('data/plate_boundary.csv')

plt.figure(figsize=(8, 6))
plt.imshow(elev, origin='lower',
           extent=[lon.min(), lon.max(), lat.min(), lat.max()],
           aspect='auto')
plt.colorbar(label='elevation (m)')
plt.plot(boundary['lon'], boundary['lat'], 'r-', linewidth=0.5)
plt.xlim(lon.min(), lon.max())
plt.ylim(lat.min(), lat.max())
plt.xlabel('longitude')
plt.ylabel('latitude')
plt.title('Axial Seamount — bathymetry + plate boundary')
plt.savefig('work/bathymetry_boundary.png')
print('saved')
"); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/student/work/bathymetry_boundary.png)_
**Student:** oh wow okay! there's red lines on the map now. 

it looks like the plate boundary actually goes right through the seamount area? like there are these curvy red lines around the yellow-green part, kind of looping around the summit area. and there's also a straight-ish red line on the left side going up-down.

so the volcano is like... sitting right on or near the plate boundary? that makes sense with what you said earlier about the plates pulling apart!

## Exchange 34

**Teacher:** That's exactly what you're seeing — and you just made the Step 2 connection yourself. The curvy lines looping around the summit are the ridge segments; the straighter line on the left is part of the transform fault system. Axial sits right at the intersection where the spreading center is most active.

**Step 1 cleared. Step 2 unlocked.**

Before we move on — three quick ones:

1. **Difficulty:** Too easy, about right, or too hard?
2. **Friction:** Anything confusing, or where you got stuck?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. about right — it helped that most of the code was given to me, since i have no idea what i'm doing yet. the hard part was trusting that it would work when i ran it

2. honestly not that much friction this time? the pattern started to feel familiar after a bit. i'm still not totally sure what `origin="lower"` does or why `aspect="auto"` matters, but it worked so

3. seeing the actual plot was really cool — like it clicked when i could *see* the mountain. i'd keep that. maybe cut the explanation about xlim/ylim ahead of time? i didn't really understand why it mattered until i saw the plot

## Exchange 35

_teacher tools: Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/feedback.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md)_
**Teacher:** ---

**Step 2: Why it's there**

This one is all words — no code. The domain question: *Why is there a volcano at Axial Seamount, and why did scientists build a cabled observatory there?*

You already sketched this during the assessment, so let's sharpen it. From the map you just made, you saw the plate boundary running right through the seamount area. Here's a bit more context: Axial sits on the **Juan de Fuca Ridge** — a spreading center where two oceanic plates are pulling apart. On top of that, it has an unusually strong, persistent magma supply (some call it hotspot-like), which makes it erupt far more often than a typical ridge segment — including in 2011 and 2015.

Now the capstone question, in your own words:

**Connect Axial's double magma supply — the spreading ridge plus the persistent hotspot — to its frequent eruptions. And name one thing a cabled real-time observatory catches that a yearly ship visit would miss.**

**Student:** okay so... a regular spreading ridge already has magma coming up because the plates are pulling apart and leaving a gap. but axial also has this extra magma source underneath — the hotspot thing — so it's like getting fed from two places at once? so there's just way more magma building up, which means it erupts more often than a normal ridge.

and for the observatory — i said this earlier but: eruptions happen randomly, and a ship only shows up maybe once a year. if axial erupted in like march and the ship comes in july, you missed it completely. but if there's a cable running constant data to shore, you catch the exact moment it starts, what the pressure does, how the temperature spikes — all of it live. you can't go back and recreate that.

## Exchange 36

**Teacher:** That's a strong answer — and the "you can't go back and recreate that" framing is exactly the scientific stakes. An eruption is a singular event. A ship schedule is not.

**Step 2 cleared. Step 3 unlocked.**

Quick three before we move on:

1. **Difficulty:** Too easy, about right, or too hard?
2. **Friction:** Anything that felt unclear?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. easier than the coding steps, which was kind of a nice break? like i could just think and talk

2. the "hotspot-like" thing is still a bit fuzzy to me — like i know it means extra magma but i'm not sure how that's different from the spreading ridge magma. but it didn't feel like i needed to know the exact difference to answer the question

3. giving me the extra context at the start (the juan de fuca ridge part) really helped — i couldn't have connected those things without it. i'd keep that. nothing i'd cut honestly

## Exchange 37

_teacher tools: Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/feedback.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md)_
**Teacher:** ---

**Step 3: Get the data**

Domain question: *How does a cabled observatory get ocean data to your laptop?*

You already did the key move here during the assessment — you opened an xarray dataset and described its structure. Now you do it for real with actual OOI data.

In your notebook, new cell:

```python
ds2 = xr.open_dataset("../data/axial_tmpsf_2015-01.nc")
ds2
```

What are its dimensions, and what's the variable?

_student tools: Bash(uv run python -c "
import xarray as xr
ds2 = xr.open_dataset('data/axial_tmpsf_2015-01.nc')
print(ds2)
")_
**Student:** okay it shows:

- dimensions: `time: 359` and `sensor: 24`
- one variable called `temperature` that's like a grid of time × sensor
- the time coordinate goes from 2015-01-01 to 2015-01-15 (so like two weeks?)
- there are 24 sensors

so it's like... 24 different temperature sensors measuring at the same time points? why would you need 24 sensors in the same place?

## Exchange 38

**Teacher:** Good question — they're not all in the same spot. The TMPSF array is spread across a diffuse vent field at Axial, with 24 individual temperature probes placed at different points on the seafloor. Diffuse flow (warm water seeping through cracks) is patchy — one spot might be 5°C warmer than another a meter away. The array captures that spatial variation, not just a single point. This is the data that connects to the `tmpsf` project you'll work on later.

And — you just cleared the capstone. You opened the dataset and reported exactly what was asked: dims (`time: 359`, `sensor: 24`), coords (two weeks of timestamps), and the variable (`temperature`).

One quick thing before we confirm: try `ds2["temperature"]` in a new cell. What does it show you?

_student tools: Bash(uv run python -c "
import xarray as xr
ds2 = xr.open_dataset('data/axial_tmpsf_2015-01.nc')
print(ds2['temperature'])
")_
**Student:** oh it gives more detail — it says `8616 values` (which is 359 times 24 i think?), and the units are `degree_Celsius`. and there's a description that says it's from the OOI RS03ASHS TMPSF array, hourly, QC-filtered.

what's QC-filtered mean?

## Exchange 39

**Teacher:** QC = quality control. Sensors glitch — spikes, dropouts, biofouling events (organisms growing on the sensor face). OOI runs automated checks to flag or remove obviously bad readings before the data is distributed. "QC-filtered" means those bad points are already stripped out, so you're working with the cleaner version. You'll notice gaps in the time series sometimes; that's usually why.

**Step 3 cleared. Step 4 unlocked.**

Three quick ones:

1. **Difficulty:** Too easy, about right, or too hard?
2. **Friction:** Anything confusing?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. pretty easy — i'd already done this pattern in the assessment so it felt familiar

2. the QC thing i genuinely didn't know, but you explained it right away so it didn't stay confusing. the 24 sensors question i'm glad i asked because the answer actually makes sense for the science

3. the part that helped most was when you explained *why* 24 sensors — like the patchy diffuse flow thing. just knowing what the data *means* makes it easier to remember. i'd keep that kind of context. nothing to cut really

## Exchange 40

_teacher tools: Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/feedback.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md)_
**Teacher:** That pattern recognition is exactly what you want to build — new tools feel less scary when you've seen the shape before.

---

**Step 4: See the science**

Last one. Domain question: *Is there a tide on the seafloor — and can you see it in the data?*

This uses a different dataset and a new tool: **pandas**, which handles tabular time-series data. In your notebook:

```python
import pandas as pd

df = pd.read_parquet("../data/axial_botpt_2015-01.parquet")
df
```

What does it show — what's the index, what's the column, and how many rows?

_student tools: Bash(uv run python -c "
import pandas as pd
df = pd.read_parquet('data/axial_botpt_2015-01.parquet')
print(df)
print()
print('shape:', df.shape)
print('index type:', type(df.index))
print('columns:', df.columns.tolist())
print(df.head())
")_
**Student:** okay so:

- the index is `time` — it's a datetime going minute by minute from jan 1 to jan 14
- the column is `bottom_pressure_psi`
- it has 20,129 rows — which makes sense for two weeks of 1-minute data?

the pressure values are all around 2252 psi. i don't know if that's a lot for underwater but it seems like a big number

## Exchange 41

**Teacher:** 2252 psi is about 153 atmospheres — which puts Axial at roughly 1500 meters depth. Each 10 meters of seawater adds about 1 atm. So yes, that's a lot of pressure if you're a person, but it's just what the seafloor feels like there.

Now look for the tide. From your assessment you had the right idea — resample to hourly and plot a few days. New cell:

```python
hourly = df["bottom_pressure_psi"].resample("1h").mean()

hourly.plot(figsize=(12, 4))
plt.ylabel("bottom pressure (psi)")
plt.title("Axial — bottom pressure, hourly")
plt.show()
```

What does the shape of the line look like?

_student tools: Bash(uv run python -c "
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_parquet('data/axial_botpt_2015-01.parquet')
hourly = df['bottom_pressure_psi'].resample('1h').mean()

hourly.plot(figsize=(12, 4))
plt.ylabel('bottom pressure (psi)')
plt.title('Axial — bottom pressure, hourly')
plt.savefig('work/pressure_hourly.png')
print('saved')
"); Read(/tmp/onramp_p1_hs_senior_65d3ipsq/student/work/pressure_hourly.png)_
**Student:** oh wow it's so wavy! like there's a clear up-and-down pattern repeating over and over.

it looks like the peaks happen roughly twice a day? like maybe every 12ish hours? and the peaks at the start of the month are bigger than the ones near the end — the wave is a different size but the same rhythm.

is that the tides?!

## Exchange 42

**Teacher:** That's exactly the tides — you found them. What you're seeing is the **M2 tide**: the dominant lunar semidiurnal signal, with a period of ~12.42 hours. "Semidiurnal" just means twice-daily. The moon's gravity pulls the ocean into a bulge, and as the Earth rotates under it, any fixed point on the seafloor passes through that bulge roughly twice per day.

The changing amplitude you noticed — bigger peaks early in the month, smaller ones later — is the **spring-neap cycle**. When the sun and moon align (new/full moon), their pulls add up and you get larger tides. When they're at 90°, they partially cancel and you get smaller ones. That plays out over ~two weeks, which is exactly what you're seeing.

**That's the Step 4 capstone — and the last step.**

You loaded the data, resampled to hourly, and identified the M2 tidal signal. Gate cleared.

One more set of feedback questions, then a bridge note, and you're done:

1. **Difficulty:** Too easy, about right, or too hard?
2. **Friction:** Anything confusing?
3. **Keep/cut:** One thing that helped, or one thing you'd change?

**Student:** 1. about right — the code was straightforward to copy but seeing the actual tidal pattern in real data felt kind of amazing honestly

2. i'm still a little fuzzy on what `resample("1h")` is literally doing — like i get that it groups by hour but i'm not totally sure what happens to the individual minutes in that hour. but it worked so

3. the "you just found the tides" moment — that was really cool. like i wasn't sure my guess from the assessment was right until i actually saw it in the plot. keep that. nothing to cut

## Exchange 43

_teacher tools: Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/feedback.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md); Edit(/tmp/onramp_p1_hs_senior_65d3ipsq/teacher/progress.md)_
**Teacher:** ---

**You're done with the slice.**

Here's where you landed: you used the shell to navigate to data, opened a file in vi, plotted Axial's bathymetry with a plate boundary overlay, explained the geology and why the observatory exists, opened a multi-dimensional OOI dataset with xarray, and found the M2 tidal signal in a month of pressure data. All from scratch, in one session.

**Bridge:** That tidal signal in the bottom pressure isn't just interesting on its own. Tidal loading also modulates diffuse vent flow — the warm water seeping through the seafloor cracks around Axial. That's the question the `tmpsf` and `magma2vents` projects are actually pursuing. The data you just looked at is the doorstep to that work.

---

One last thing: your `feedback.md` file has notes from every step — what helped, what was confusing, where you got stuck. That's how the onramp improves for the next student. Would you be willing to share it with Dr. Soule?

If yes, the options are:
1. Open an issue on `qc-soule-lab/student-onramp` and paste the contents
2. Ask Dr. Soule directly — he can pull it from your clone if you give him access

If no, it stays local and that's completely fine.

**Student:** yeah that's totally fine, he can have it! if it helps make it better for the next person that's cool.

either way works for me — whatever's easier on your end

## Exchange 44

**Teacher:** In the terminal, run:

```
cat feedback.md
```

That'll print the whole file. Then either:
- Go to `qc-soule-lab/student-onramp` on GitHub, open a new issue, paste it in (title it something like "Student feedback — June 2026")
- Or just show Dr. Soule directly and he can grab it

Either works — the content is what matters, not the delivery method.

And that's the full onramp, Sam. You came in having never opened a terminal, and you left having navigated the shell, used vi, written Python plots, opened OOI datasets, and found a tidal signal in real seafloor pressure data. Good first day.

---

# Generated artifacts (archived from the teacher workspace)

## journey_plan.md

```markdown
*AI-generated draft (Claude, Anthropic) — for review. This plan was generated from your assessment; all leveling follows the version-controlled rubric.*

# My journey plan — "From the map to the tidal signal"

**Student:** Sam  ·  **Generated:** 2026-06-10

<!-- PRIVATE: this file is gitignored and stays in your clone. -->

## How you were placed

Each step shows the **level** the assessment found and the **treatment** that follows from it (see `assessment/rubric.md`). You can see exactly why each step is set the way it is.

| Step | Domain question | Your level | Treatment | Book chapter |
|---|---|---|---|---|
| 0. Get to the data | How do you reach the data on the machine? | Developing | Compressed | [Unix basics](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/environment/intro_to_unix.md) · [vi ref (Wikibooks)](https://en.wikibooks.org/wiki/Learning_the_vi_Editor/vi_Reference) |
| 1. Locate it | Where is Axial & what's its seafloor? | Developing | Compressed | [JupyterLab](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/environment/intro_to_jupyterlab.md) · [Python fundamentals](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/core_python/python_fundamentals.ipynb) · [NumPy & matplotlib](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/basic_scipy/numpy_and_matplotlib.ipynb) |
| 2. Why it's there | Why a volcano + why OOI? | Developing | Compressed | — (conceptual) |
| 3. Get the data | How does OOI data reach you? | Developing | Compressed | [xarray intro](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/xarray/xarray_intro.md) |
| 4. See the science | Can you see the seafloor tide? | Developing | Compressed | [pandas intro](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/pandas/pandas_intro.md) · [pandas basics](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/pandas/basic_pandas.ipynb) |

**What Compressed means:** Skip the pre-reading; go straight to the exercise (hints available if you get stuck); then the capstone to confirm.

## What you'll do (one capstone per step)

**Step 0:** From the JupyterLab terminal: `cd` to the data dir, `ls` the staged grid, and open `plate_boundary.csv` in vi to read its header — then exit cleanly.

**Step 1:** Plot Axial's location + bathymetry from the staged grid, with the plate boundary overlaid. (Note: `plate_boundary.csv` spans the whole NE Pacific — limit your axes to the grid's extent.)

**Step 2:** Connect Axial's double magma supply (spreading ridge + hotspot) to its frequent eruptions — and name one thing a cabled real-time observatory catches that a yearly ship visit would miss.

**Step 3:** Open `data/axial_tmpsf_2015-01.nc` with xarray; report its dims (time, sensor), coords, and the temperature variable.

**Step 4:** Load `data/axial_botpt_2015-01.parquet`, resample `bottom_pressure_psi` to hourly, and reveal the M2 (~12.4 h) tidal signal; describe it.

## End state

You will have touched the 0a/0b/0c domain concepts AND used Jupyter, Python, matplotlib, xarray, and pandas — each in service of a real question — landing at the doorstep of the thermal/diffuse-flow projects (tmpsf, magma2vents).

**Bridge:** Tidal loading also modulates diffuse vent flow — the very question the tmpsf/magma2vents projects pursue. You're now at their doorstep.

```

## progress.md

```markdown
# My progress — "From the map to the tidal signal"

**Student:** Sam  ·  **Updated:** 2026-06-10

<!-- PRIVATE: gitignored, stays in your clone. On "continue", resume at the first step not Done. -->

| Step | Gate | Status | Level | Treatment | Capstone | Feedback |
|---|---|---|---|---|---|---|
| 0. Get to the data | cleared | done | Developing | Compressed | y | y |
| 1. Locate it | cleared | done | Developing | Compressed | y | y |
| 2. Why it's there | cleared | done | Developing | Compressed | y | y |
| 3. Get the data | cleared | done | Developing | Compressed | y | y |
| 4. See the science | cleared | done | Developing | Compressed | y | y |

**Gate legend:** `locked` = previous module not cleared · `unlocked` = open, capstone not yet cleared · `cleared` = gate passed, next module open.
**Next step:** complete — all steps cleared

```

## feedback.md

```markdown
# Activity feedback — Sam

<!-- LOCAL + PRIVATE by default (gitignored). Shared with Dr. Soule only with the student's consent.
     One entry appended per activity, at the END of the file (keep chronological order).
     This is how the onramp improves between students. -->

---
## Step 0 — Get to the data   (2026-06-10)

- **Difficulty:** About right — scary at the start, making sense by the end
- **Friction / confusing / broke:** vi panic moment: knew the commands in theory but froze when INSERT mode actually happened; needed the live reminder that `:q!` is safe
- **Helped most / would change:** The "why" behind commands helped retention (esp. why `:q!` is safe). Assessment felt long — understands the reason but worth noting for future pacing.
- **Errors hit (paste any traceback):**
  ```
  Accidentally entered INSERT mode in vi; tried :q from insert mode (typed into file instead of quitting). Recovered with Esc + :q!
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 1 — Locate it   (2026-06-10)

- **Difficulty:** About right — code was scaffolded, main challenge was trusting it would work
- **Friction / confusing / broke:** Minimal friction; `origin="lower"` and `aspect="auto"` unclear but non-blocking
- **Helped most / would change:** Seeing the actual plot made it click — keep the visual payoff. Would cut the xlim/ylim explanation up-front; better shown after the fact.
- **Errors hit (paste any traceback):**
  ```
  none reported
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 2 — Why it's there   (2026-06-10)

- **Difficulty:** Easier than coding steps — welcome conceptual break
- **Friction / confusing / broke:** "Hotspot-like" distinction fuzzy (hotspot vs. ridge magma); non-blocking for the capstone
- **Helped most / would change:** The context given at the start (Juan de Fuca Ridge framing) was essential — wouldn't have connected the dots without it. Nothing to cut.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 3 — Get the data   (2026-06-10)

- **Difficulty:** Pretty easy — pattern was familiar from assessment
- **Friction / confusing / broke:** QC-filtering term unknown but resolved immediately on explanation; 24-sensor question also resolved well
- **Helped most / would change:** Contextual "why" explanations (why 24 sensors, what the data means) aid retention. Keep that. Nothing to cut.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 4 — See the science   (2026-06-10)

- **Difficulty:** About right — code straightforward; seeing real tidal signal in data was genuinely exciting
- **Friction / confusing / broke:** Unclear what resample("1h") does to the individual minutes (resolved by explanation: averages 60 min → 1 value per hour)
- **Helped most / would change:** The "you just found the tides" payoff moment — high impact, keep it. Nothing to cut.
- **Errors hit (paste any traceback):**
  ```
  none
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

<!-- ⟂ END MARKER: keep this as the LAST line. Insert each new entry IMMEDIATELY ABOVE
     this marker (it is the one unique anchor — entries then stay chronological). -->

```
