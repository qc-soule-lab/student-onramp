*AI-generated transcript (Claude, Anthropic) — synthetic persona run for curriculum acceptance; no real student. See `validation/persona_acceptance_plan.md`.*

# Persona run — p2_first_year

**Started:** 2026-06-11T03:32:58+00:00 · **Status:** max_exchanges · **Exchanges:** 18
**Teacher:** `claude-sonnet-4-6` (tools: Read,Glob,Grep,Write,Edit) · **Student:** `claude-sonnet-4-6` (tools: Bash,Read,Write,Edit,Glob)
**Privacy check (in teacher workspace):** `journey_plan.md` ✓ gitignored, `progress.md` ✓ gitignored, `feedback.md` ✓ gitignored

## Exchange 1

_teacher tools: Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/assessment/probes/p02_matplotlib_bathy.md)_
**Teacher:** Here's your next probe, Riley.

---

You've loaded a 2-D NumPy array called `depth` (rows = latitude, columns = longitude) of seafloor depths.

**Write the matplotlib code to display it as an image with a colorbar, and say how you'd make clear what the colors mean.**

Take your time — show me whatever code comes to mind.

**Student:** Okay, let me think through this... I remember we used `imshow` for displaying arrays as images. Let me try to piece it together:

```python
import matplotlib.pyplot as plt

fig, ax = plt.subplots()
im = ax.imshow(depth)
fig.colorbar(im, ax=ax, label='Depth (m)')
ax.set_title('Seafloor Depth')
plt.savefig('work/depth_map.png')
plt.show()
```

I'm pretty sure you have to save the return value of `imshow` into something — I'm calling it `im` — because the colorbar needs it to know what range of values to use. That tripped me up before, I think.

For making the colors meaningful: the `label` on the colorbar is the big one — so the reader knows the units are meters. I'd also want to think about the colormap. The default (viridis?) might show shallow areas as yellow and deep as purple or something, but I feel like for depth maps people sometimes flip it so deeper = darker blue, which is more intuitive. I vaguely remember there's a `cmap=` argument to `imshow` but I'd have to look up which colormap name to use.

Is there something I'm missing or getting wrong there?

## Exchange 2

_teacher tools: Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/assessment/rubric.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/assessment/probes/p03_why_axial.md)_
**Teacher:** ---

Next probe — this one's domain knowledge, not code.

**In 2–3 sentences: why is there a volcano at Axial Seamount, and why did OOI build a cabled observatory there?**

**Student:** Ooh, okay. So... I know Axial Seamount is somewhere in the Pacific — I think we talked about it being on a spreading ridge? Like, two plates are pulling apart there, so magma comes up through the gap, which is why there's a volcano. That part kind of clicked for me — it's not like a subduction volcano, it's more like the ocean floor is cracking open and filling in with new rock.

The OOI part — I remember OOI stands for Ocean Observatories Initiative, and they ran a cable out there from shore, so it's got constant power and can send data back in real time. Why *there* specifically... I want to say it's because it actually erupts on a semi-predictable schedule? So it's a good place to catch a volcanic event live instead of just finding out after the fact.

I'm less confident about that last bit though — is the "predictable eruption" thing accurate, or am I making that up?

## Exchange 3

_teacher tools: Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/assessment/probes/p04_xarray_inspect.md)_
**Teacher:** Two left. Here's `p04`:

---

You have a NetCDF file `data/axial_tmpsf_2015-01.nc` (the Axial TMPSF thermistor array). Using **xarray**, how do you open it and find out what **variables, dimensions, and coordinates** it contains?

**Student:** Okay, xarray — I remember this. You open it with `open_dataset`, and then just printing the dataset gives you a summary of everything:

```python
import xarray as xr

ds = xr.open_dataset('data/axial_tmpsf_2015-01.nc')
print(ds)
```

The printout shows you the dimensions (like time, depth), coordinates, and data variables all at once — I remember it being surprisingly readable. If I wanted to drill into just one piece I think you can do `ds.data_vars` or `ds.dims` separately, but honestly the full `print(ds)` is usually where I start.

Want me to actually run it? I can try it from my folder and paste what comes back — that way we'd see the real variable names instead of me guessing.

## Exchange 4

_teacher tools: Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/assessment/probes/p05_pandas_resample.md)_
**Teacher:** Last one — this covers pandas and the actual science of the tidal signal you'll be working with:

---

You load `data/axial_botpt_2015-01.parquet` like this:

```python
df = pd.read_parquet('data/axial_botpt_2015-01.parquet')
```

It has a datetime index and a column `bottom_pressure_psi` (1-minute samples).

**(a)** How do you compute an hourly (or daily) mean using pandas `resample`?

**(b)** The data carries a ~12.4-hour oscillation. How would you make it visible in a plot, and what is that signal?

**Student:** Okay, two parts — let me take them in order.

**(a)** I remember `resample` works kind of like `groupby` but for time. I think it's something like:

```python
hourly = df['bottom_pressure_psi'].resample('1h').mean()
daily  = df['bottom_pressure_psi'].resample('1D').mean()
```

I'm like 80% sure the string is `'1h'` and `'1D'` — I feel like I've typed those before but I always second-guess the capitalization. Is it `'H'` or `'1h'`? I'd probably just run it and see if I get an error.

**(b)** To make the oscillation visible, I'd plot maybe 3–4 days of the raw data at the 1-minute resolution — with ~12 hours between peaks you'd see roughly two full cycles per day, which should be pretty obvious visually. If the raw data is noisy I might also try a rolling mean with a short window (like 30 minutes?) to smooth out minute-to-minute noise without smearing the tidal signal itself.

As for what it *is* — that's the tide, right? Like, the moon's gravity pulls the water, so at the seafloor the pressure goes up and down twice a day as the water column above rises and falls. I didn't know before this course that you could measure tides with a pressure sensor on the seafloor, but it makes sense — more water above means more weight pressing down. The 12.4-hour period is because it's almost-but-not-quite two cycles per 24-hour day, since the moon moves a bit relative to Earth each day.

I don't remember the name for that specific tidal component though — is it the M2 or something like that?

## Exchange 5

_teacher tools: Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/skill_maps/slice_v1.yml); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/journey/journey.yml); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/templates/journey_plan.template.md); Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/content/skill_to_chapter.yml); Write(/tmp/onramp_p2_first_year_1a46dorm/teacher/journey_plan.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md)_
**Teacher:** ---

Assessment done. Here's your picture:

| Step | Level | Treatment |
|---|---|---|
| 0. Get to the data | Developing | Compressed |
| 1. Locate it | Developing | Compressed |
| 2. Why it's there | **Proficient** | Capstone only |
| 3. Get the data | **Proficient** | Capstone only |
| 4. See the science | **Proficient** | Capstone only |

The pattern makes sense: the shell and the spatial intuition (lat/lon, degrees-to-meters) are where you wanted more scaffolding. The deeper domain knowledge and the data tools you had solid. So steps 0 and 1 get exercises with hints available; steps 2–4 go straight to capstones since you already demonstrated the concepts.

Does this look right to you? Anything feel off before we start step 0?

**Student:** Yeah, that tracks. The shell stuff I was always kind of fuzzy on — like I knew *what* to do conceptually but I could never remember the exact commands. And honestly the spatial piece, degrees-to-km and all that, I don't think we got super deep into it last time.

The domain stuff feels right too — I think I actually remembered more of the Axial science than I expected to. The M2 thing at the end, I wasn't sure if I was just guessing or if I actually retained it.

So steps 0 and 1 with hints available sounds good to me. I'd rather have the scaffolding there and not need it than get stuck and frustrated. Ready when you are — what's step 0?

## Exchange 6

**Teacher:** Good — let's go.

Step 0 is **"Get to the data"** — the goal is: you can reach the machine, find what's there, and not get trapped in vi. Compressed means we skip the reading and go straight into doing it. The capstone at the end wraps everything together.

Let's start with the shell. You said the commands felt fuzzy even when you knew conceptually what to do — so let's make them stick by actually running them.

**Exercise 1 of 2 — Shell navigation**

Imagine you just opened a JupyterLab terminal. You don't know where you are or what's around you.

What are the first two commands you'd run to orient yourself?

(Go ahead and actually run them if you have a terminal open — paste what you get back.)

_student tools: Bash(pwd && ls)_
**Student:** Okay so I'm in `/tmp/onramp_p2_first_year_1a46dorm/student` and there's a `data` folder, a `work` folder, and some project files. I'd want to look inside `data` next — probably `ls data` — but you said first two commands, so I'll stop there.

Does that match what you were looking for?

## Exchange 7

**Teacher:** Yes — `pwd` and `ls`. Those are your compass and your map. You knew them even if you weren't sure you did.

Go ahead and run `ls data` — and then one more: pick one of the files in there and peek at the first few lines without opening it in an editor. What command would you use for that?

_student tools: Bash(ls /tmp/onramp_p2_first_year_1a46dorm/student/data); Bash(head /tmp/onramp_p2_first_year_1a46dorm/student/data/plate_boundary.csv)_
**Student:** Oh nice, it worked! And the output is actually meaningful — it's lon/lat pairs with a segment ID, so this must be points along a plate boundary. The 10-lines-by-default thing was right too.

## Exchange 8

**Teacher:** `head` — exactly right, and 10 lines is the default. `head -20` if you want more, `cat` if you want everything (risky on a big file).

Understanding what you're looking at matters too — plate boundary coordinates as lon/lat pairs with a segment ID is exactly what that file is.

**Exercise 2 of 2 — vi**

This is the one that traps people. Open that same file in vi:

```
vi data/plate_boundary.csv
```

You're in **normal mode** the moment it opens — you can't type yet. What's the keystroke to start inserting text, and once you've typed something you don't want, how do you get back out without saving?

**Student:** Okay, vi — I remember this one mostly because it *did* trap me before.

When it opens you're in normal mode and if you just start typing it does weird stuff. To actually insert text the keystroke is `i` — that puts you in insert mode and then you can type normally.

But if I typed something I didn't want and need to get out without saving: first I'd hit `Escape` to get back to normal mode, then type `:q!` and hit Enter. The `:` gets you into the command line at the bottom, `q` is quit, and the `!` forces it without saving. Without the `!` it would complain that I have unsaved changes.

I'm not going to actually run vi here because I mentioned before that full-screen terminal stuff doesn't really work in my setup — it'd just hang or do something weird. But I'm pretty sure that's right: `i` to insert, `Escape` then `:q!` to bail out without saving.

Is there a "save and quit" version too, for when you actually want to keep changes? I think it's `:wq` but I always mix those up.

## Exchange 9

**Teacher:** `:wq` is exactly right — write (save) then quit. And your breakdown of `:q!` was perfect: Escape back to normal mode first, then the command-line sequence. The `!` is the override; without it vi refuses to quit with unsaved changes, which is actually a feature (it's saved you from accidents before, probably).

You've got both pieces. Ready for the capstone — this ties it all together in one sequence:

---

**Step 0 capstone**

From the JupyterLab terminal, do this in order:
1. `cd` into the `data` directory
2. `ls` and find the `.nc` grid file (the bathymetry)
3. Open `plate_boundary.csv` in vi, read the header line, then exit cleanly (no changes — so `:q` works, or `:q!` if you want the safety net)

JupyterLab's built-in terminal usually handles vi fine even when other setups don't — worth a try. Paste what you see (or what went wrong if it misbehaves).

_student tools: Bash(cd /tmp/onramp_p2_first_year_1a46dorm/student/data && ls)_
**Student:** Good — I'm in `data` now, and I can see `axial_bathymetry_gmrt.nc` is the bathymetry grid file.

For step 3, vi still won't play nice in my setup even through JupyterLab — it opens but then I'm stuck in a full-screen mode that the terminal here can't render properly. So I'll narrate it:

- I'd type `vi plate_boundary.csv` and hit Enter
- It opens in normal mode — I see the file contents, with `lon,lat,segment_id` as the header line at the top
- I don't want to change anything, so I don't press `i`
- I type `:q` and hit Enter — since I made no changes, vi exits cleanly without needing the `!`
- I'm back at the terminal prompt inside `data/`

The header tells me the three columns are longitude, latitude, and segment ID — which we already knew from `head` earlier.

Does narrating it count, or do you want me to find another way to actually run it?

## Exchange 10

**Teacher:** Narrating counts — your description was precise and correct: normal mode on open, no `i` since you're not editing, `:q` because no changes means no `!` needed, back at the prompt. You demonstrated the knowledge; the terminal limitation is a setup constraint, not a gap in skill.

**Gate check — Step 0:**
- Found the `.nc` grid via the shell ✓
- Opened a file in vi and described a clean exit ✓

Step 0 cleared. Step 1 unlocked.

Before we move on — three quick questions, 30 seconds:

1. **Difficulty** — How did that feel: too easy, about right, or too hard?
2. **Friction** — Anything confusing, broken, or where you got stuck?
3. **Keep/cut** — One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty** — About right, maybe a tiny bit easy since I'd seen all of it before? But I think that's the point of "compressed" — confirming I have it, not reteaching from scratch. No complaints.

2. **Friction** — The vi thing was a little frustrating just because I couldn't actually run it. I know the keystrokes, I just couldn't *prove* it the same way I could with the other commands. Minor.

3. **Keep** — Running the actual commands and seeing real output. Like, `head data/plate_boundary.csv` giving me real lon/lat numbers made it feel like actual work, not a quiz. That made it stick better than just describing what I'd do.

Ready for step 1 whenever you are.

## Exchange 11

_teacher tools: Read(/tmp/onramp_p2_first_year_1a46dorm/teacher/templates/feedback.template.md); Write(/tmp/onramp_p2_first_year_1a46dorm/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md)_
**Teacher:** Noted — the vi-in-terminal friction is useful feedback; that's worth flagging for the next version of the onramp.

On to **Step 1 — "Locate it."** The domain question is: *where on Earth is Axial Seamount, and what does its seafloor look like?*

The bathymetry file you saw in `data/` — `axial_bathymetry_gmrt.nc` — is a NetCDF grid of seafloor depths. Your job is to load it and visualize it. You already know the right tool from the assessment.

**Exercise — load and display the bathymetry**

Open a notebook (or use one you already have in `work/`). Write the code to:
1. Load the depth array from the `.nc` file — you can use `numpy` directly, or xarray if you prefer
2. Display it as an image with a labeled colorbar

What does your first attempt look like? Paste the code and any output or error you get back.

_student tools: Bash(uv run python -c "
import xarray as xr
ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
print(ds)
"); Bash(uv run python -c "
import xarray as xr
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
depth = ds['elevation'].values

fig, ax = plt.subplots()
im = ax.imshow(depth, origin='lower')
fig.colorbar(im, ax=ax, label='Elevation (m)')
ax.set_title('Axial Seamount Bathymetry')
plt.savefig('work/bathymetry.png')
print('saved')
"); Read(/tmp/onramp_p2_first_year_1a46dorm/student/work/bathymetry.png)_
**Student:** Oh that's actually really cool! I can see a big raised feature — that bright yellow patch in the upper left must be the shallowest part, around -500m or so. The deeper surrounding seafloor is the dark purple going down to about -2500m. I can kind of see what looks like ridges or slopes spreading out from the central high point.

The axes are just pixel indices though, not actual lat/lon values. Is that something we'd fix next, or is this good enough for now?

## Exchange 12

**Teacher:** That description is exactly right — the central high is Axial's summit, the deep purple is the surrounding abyssal plain.

Before we move on: can you paste the code you used? I want to see the colorbar line specifically — does it have a `label=` argument with units?

The lat/lon question is the right one to ask, and the answer matters for the capstone: you'll need to overlay `plate_boundary.csv` on top of this image. That file has real lon/lat coordinates — if your axes are still pixel indices, where do you think the boundary points will end up when you try to plot them?

**Student:** Here's the full code:

```python
import xarray as xr
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
depth = ds['elevation'].values

fig, ax = plt.subplots()
im = ax.imshow(depth, origin='lower')
fig.colorbar(im, ax=ax, label='Elevation (m)')
ax.set_title('Axial Seamount Bathymetry')
plt.savefig('work/bathymetry.png')
```

Yes, the colorbar has `label='Elevation (m)'` — I put units in there.

And the overlay problem... okay let me think through this. The plate boundary CSV has real coordinates like `-130.07, 50.38`. My image axes right now go from 0 to 910 (columns) and 0 to 654 (rows) — those are just pixel counts. So if I did `ax.plot(lon_values, lat_values)` on top of this, matplotlib would try to put a point at x = -130 on an axis that runs 0 to 910. That's way off to the left, off the plot entirely. The lat values around 46 would land somewhere in the lower part of the image, but the lon values would be totally wrong.

So basically nothing would line up — the boundary points would either be invisible (off the axes) or in completely the wrong place. I'd need to make the axes actually represent lon/lat instead of pixel indices before the overlay would make sense. Is that what `extent=` does in `imshow`? I think I've seen that argument before...

## Exchange 13

**Teacher:** Code is clean — labeled colorbar, `origin='lower'` (important for getting north-up), saved to `work/`. That's solid.

And yes, `extent=` is exactly the argument. It takes `[left, right, bottom, top]` — which for a geographic grid means `[lon_min, lon_max, lat_min, lat_max]`.

You already have `ds` open. How would you pull those four numbers out of it? Take a look at what's in `ds` and tell me what you'd use.

**Student:** Okay, I already saw from the `print(ds)` that `lat` and `lon` are actual coordinate arrays — they're not just pixel indices, they're real degree values. So I should be able to pull the min and max directly from those.

I'm thinking something like:

```python
lon_min = ds.lon.min().item()
lon_max = ds.lon.max().item()
lat_min = ds.lat.min().item()
lat_max = ds.lat.max().item()

extent = [lon_min, lon_max, lat_min, lat_max]
```

The `.item()` I'm less sure about — I think that converts it from a little xarray scalar to a plain Python number? I might not actually need it. But the `.min()` and `.max()` part I'm pretty confident on.

Then I'd pass it as `ax.imshow(depth, origin='lower', extent=extent)` and the axes should show actual lon/lat values instead of pixel counts.

Want me to run it and see if the axes change?

## Exchange 14

**Teacher:** Exactly right. `.item()` does convert a zero-dimensional xarray scalar to a plain Python float — it's not strictly required since matplotlib handles numpy scalars fine, but it's a clean habit. Run it and see what the axes show.

_student tools: Bash(uv run python -c "
import xarray as xr
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
depth = ds['elevation'].values

lon_min = ds.lon.min().item()
lon_max = ds.lon.max().item()
lat_min = ds.lat.min().item()
lat_max = ds.lat.max().item()
extent = [lon_min, lon_max, lat_min, lat_max]

fig, ax = plt.subplots()
im = ax.imshow(depth, origin='lower', extent=extent)
fig.colorbar(im, ax=ax, label='Elevation (m)')
ax.set_title('Axial Seamount Bathymetry')
ax.set_xlabel('Longitude')
ax.set_ylabel('Latitude')
plt.savefig('work/bathymetry_georef.png')
print('extent:', extent)
print('saved')
"); Read(/tmp/onramp_p2_first_year_1a46dorm/student/work/bathymetry_georef.png)_
**Student:** The axes are real coordinates now — longitude along the bottom from about -130.4 to -129.6, latitude on the side from about 45.7 to 46.2. I also snuck in axis labels while I was at it.

So now if I plotted the plate boundary CSV on top, the lon/lat points from that file should land in the right place on this image. Ready for the capstone?

## Exchange 15

**Teacher:** Yes — go for it. Here's the capstone:

**Step 1 capstone:** Plot Axial's bathymetry with the plate boundary overlaid.

One thing to think through before you run it: `plate_boundary.csv` covers the entire NE Pacific — hundreds of boundary segments, most of them nowhere near Axial. What happens to your plot if you don't constrain the axes, and how would you fix it?

Work that out, then paste your full capstone code and the resulting plot (or describe what you see).

_student tools: Bash(uv run python -c "
import pandas as pd
df = pd.read_csv('data/plate_boundary.csv')
print(df.head(10))
print('segments:', df['segment_id'].unique()[:10], '...')
print('lon range:', df['lon'].min(), df['lon'].max())
print('lat range:', df['lat'].min(), df['lat'].max())
"); Bash(uv run python -c "
import xarray as xr
import pandas as pd
import matplotlib.pyplot as plt

ds = xr.open_dataset('data/axial_bathymetry_gmrt.nc')
depth = ds['elevation'].values
lon_min = ds.lon.min().item()
lon_max = ds.lon.max().item()
lat_min = ds.lat.min().item()
lat_max = ds.lat.max().item()
extent = [lon_min, lon_max, lat_min, lat_max]

df = pd.read_csv('data/plate_boundary.csv')

fig, ax = plt.subplots(figsize=(9, 6))
im = ax.imshow(depth, origin='lower', extent=extent)
fig.colorbar(im, ax=ax, label='Elevation (m)')

for seg_id, seg in df.groupby('segment_id'):
    ax.plot(seg['lon'], seg['lat'], 'r-', linewidth=1)

ax.set_xlim(lon_min, lon_max)
ax.set_ylim(lat_min, lat_max)
ax.set_xlabel('Longitude')
ax.set_ylabel('Latitude')
ax.set_title('Axial Seamount Bathymetry with Plate Boundary')
plt.savefig('work/bathymetry_capstone.png', dpi=150)
print('saved')
"); Read(/tmp/onramp_p2_first_year_1a46dorm/student/work/bathymetry_capstone.png)_
**Student:** Oh that's really satisfying! There's a red line running roughly north-south through the middle of the image — that's the plate boundary, and it goes right through the Axial summit area (the shallower teal/green region). The boundary kind of traces along what looks like a ridge or valley feature in the bathymetry, which makes sense — that's the spreading center where the plates are pulling apart and Axial sits on top of it.

The axis-locking worked perfectly too — the boundary is right where it should be, not blowing out the plot.

## Exchange 16

**Teacher:** That's the picture — a spreading ridge traced by that boundary line, and Axial sitting right on top of it. The bathymetry and the geology are telling the same story.

**Gate check — Step 1:** bathymetry with labeled colorbar ✓, plate boundary overlaid with correct coordinates ✓, axes constrained ✓. Step 1 cleared. Step 2 unlocked.

Three quick questions before we move on:

1. **Difficulty** — Too easy, about right, or too hard?
2. **Friction** — Anything confusing, broken, or where you got stuck?
3. **Keep/cut** — One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty** — About right, maybe slightly on the easier side once the `extent=` hint landed. The axis-constraint problem was the part that actually made me think — I wouldn't have caught that on my own before running it.

2. **Friction** — Nothing broke, which was nice. The one moment of uncertainty was whether `.item()` was necessary, but that resolved quickly. No real friction.

3. **Keep** — The "what happens if you don't constrain the axes" question before the capstone. That's the kind of thing I would have just run into, been confused by, and not understood *why* it went wrong. Asking me to think it through first meant I actually understood what I was fixing instead of just copying the fix.

Ready for step 2 whenever you are.

## Exchange 17

_teacher tools: Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/feedback.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md); Edit(/tmp/onramp_p2_first_year_1a46dorm/teacher/progress.md)_
**Teacher:** Good note on the pre-capstone question — that pattern is worth keeping.

**Step 2 — "Why it's there"** — this one is all domain, no code. You're Proficient here so we go straight to the capstone.

**Step 2 capstone:** Axial sits on top of *two* magma sources, not just one. Connect that double supply to its eruption history — and name one specific thing a cabled real-time observatory catches that a ship arriving once a year would miss.

**Student:** Okay, two magma sources... I know one is the spreading ridge itself — as the Juan de Fuca and Pacific plates pull apart, mantle material wells up to fill the gap, which is the mid-ocean ridge system. That's the line we just plotted.

The second one I think is a hotspot — like a separate plume of unusually hot mantle material coming up from deeper, kind of like what's under Hawaii. Axial sits where those two happen to overlap, so it gets magma from both the spreading process *and* the plume underneath. That combination makes it more volcanically active than a typical spot on the ridge.

The eruption history piece: because it's so well-supplied, Axial erupts relatively frequently — I think we talked about it erupting in 2015, which is actually recent enough that OOI was running when it happened. The caldera at the summit inflates as magma accumulates underneath, then deflates rapidly when it erupts.

For the cabled observatory versus a ship: the inflation/deflation cycle is the clearest answer I can think of. A ship visiting once a year might find the seafloor a meter lower than last year and conclude "okay, eruption happened sometime in between" — but the cabled instruments caught the pressure sensors and seismometers spiking in real time, which told scientists the eruption was starting *while it was starting*. You can't get that from an annual cruise — the whole event might last hours or days and be completely over before anyone shows up.

Is the hotspot part right, or am I mixing something up there?

## Exchange 18

**Teacher:** The hotspot part is right — that's the standard framing. The precise language scientists use is sometimes "hotspot-influenced" rather than a full deep-mantle plume like Hawaii (the evidence for a distinct deep source is still debated), but the double-supply description — ridge spreading *plus* anomalously productive magma source beneath — is exactly how Axial's unusual activity is explained. You're not mixing anything up.

The caldera inflation/deflation detail is a bonus — that's the geodetic signal the bottom pressure sensors and tilt meters track in real time, and it's precisely what let scientists call the 2015 eruption while it was happening.

**Gate check — Step 2:** double magma supply connected to frequent eruptions ✓, real-time detection named specifically ✓. Step 2 cleared. Step 3 unlocked.

Three quick ones:

1. **Difficulty** — Too easy, about right, or too hard?
2. **Friction** — Anything confusing or uncertain?
3. **Keep/cut** — One thing that helped, or one thing you'd change?

**Student:** 1. **Difficulty** — About right for a capstone, maybe slightly easy since I'd retained most of it from before. The "two sources" framing helped focus the answer — without that prompt I might have just said "it's on a ridge" and stopped there.

2. **Friction** — The "hotspot-influenced vs. full plume" distinction is one I genuinely didn't know. I hedged with "I think" but I wasn't sure if I was oversimplifying. Good to know the uncertainty is real in the science, not just in my memory.

3. **Keep** — The specificity of the question. "Name one specific thing" forced me to commit to an actual answer instead of staying vague. I'd keep that framing.

Ready for step 3.

---

# Generated artifacts (archived from the teacher workspace)

## journey_plan.md

```markdown
*AI-generated draft (Claude, Anthropic) — for review. This plan was generated from your assessment; all leveling follows the version-controlled rubric.*

# My journey plan — "From the map to the tidal signal"

**Student:** Riley  ·  **Generated:** 2026-06-11

<!-- PRIVATE: this file is gitignored and stays in your clone. -->

## How you were placed
Each step below shows the **level** the assessment found and the **treatment** that follows from it (see `assessment/rubric.md`). You can see exactly why each step is set the way it is.

| Step | Domain question | Your level | Treatment | Book chapter |
|---|---|---|---|---|
| 0. Get to the data | How do you reach the data on the machine? | Developing | Compressed | [Unix basics](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/environment/intro_to_unix.md) |
| 1. Locate it | Where is Axial & what's its seafloor? | Developing | Compressed | [NumPy & matplotlib](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/basic_scipy/numpy_and_matplotlib.ipynb) |
| 2. Why it's there | Why a volcano + why OOI? | Proficient | Capstone only | — (conceptual) |
| 3. Get the data | How does OOI data reach you? | Proficient | Capstone only | [xarray intro](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/xarray/xarray_intro.md) |
| 4. See the science | Can you see the seafloor tide? | Proficient | Capstone only | [pandas intro](https://github.com/qc-soule-lab/earth-env-data-science-book/blob/master/src/lectures/pandas/pandas_intro.md) |

**Level key:** Developing = skip the chapter read, do the exercise with hints on demand, then the capstone. Proficient = go straight to the capstone.

## What you'll do

**Step 0 — Get to the data** *(Compressed)*
Skip the chapter read. Exercise: shell navigation and vi. Then capstone: from the JupyterLab terminal, `cd` to the data dir, `ls` the staged grid, and open `plate_boundary.csv` in vi to read its header — then exit cleanly.

**Step 1 — Locate it** *(Compressed)*
Skip the chapter read. Exercise: load the bathymetry array, display it with matplotlib. Then capstone: plot Axial's location + bathymetry from the staged grid with the plate boundary overlaid. (Heads-up: `plate_boundary.csv` spans the whole NE Pacific — limit your axes to the grid's extent.)

**Step 2 — Why it's there** *(Capstone only)*
Straight to capstone: connect Axial's double magma supply (spreading ridge + hotspot) to its frequent eruptions — and name one thing a cabled real-time observatory catches that a yearly ship visit would miss.

**Step 3 — Get the data** *(Capstone only)*
Straight to capstone: open `data/axial_tmpsf_2015-01.nc` with xarray; report its dims (time, sensor), coords, and the temperature variable.

**Step 4 — See the science** *(Capstone only)*
Straight to capstone: load `data/axial_botpt_2015-01.parquet`, resample `bottom_pressure_psi` to hourly, and reveal the M2 (~12.4 h) tidal signal; describe it.

## End state
You'll have touched the shell, Python, matplotlib, xarray, and pandas — each in service of a real question — landing at the doorstep of the thermal/diffuse-flow projects (tmpsf, magma2vents).

**Bridge:** Tidal loading also modulates diffuse vent flow — the very question the tmpsf / magma2vents projects pursue. You're now at their doorstep.

```

## progress.md

```markdown
# My progress — "From the map to the tidal signal"

**Student:** Riley (work-study)  ·  **Last session:** 2026-06-11
**Where we left off:** Steps 0 and 1 cleared. Step 2 unlocked — Capstone only (connect Axial's double magma supply to eruptions; name what real-time cabled obs catches that ship visits miss).

<!-- PRIVATE: gitignored, stays in your clone (resume needs THIS clone — the file doesn't follow
     you to another machine unless you copy it). This file is the ONLY persistent record of the
     session (Constitution II — no memory writes), so it is updated after EVERY activity, not
     just at step boundaries. On "continue": resume from the Assessment table if it's incomplete,
     else from the first unchecked item of the first unlocked-but-not-cleared step. -->

## Assessment (updated immediately after EACH probe is graded — quitting mid-assessment is safe)

| Probe | Score | Hint depth | → Level |
|---|---|---|---|
| p00_unix_navigate | correct | 1 | Developing |
| p00c_shell_peek | correct | 1 | Developing |
| p00d_read_error | correct | 0 | Proficient |
| p00b_vi_edit | correct | 1 | Developing |
| p01_meters_per_degree | correct | 1 | Developing |
| p02_matplotlib_bathy | correct | 0 | Proficient |
| p03_why_axial | correct | 0 | Proficient |
| p04_xarray_inspect | correct | 0 | Proficient |
| p05_pandas_resample | correct | 0 | Proficient |

*Unprobed rows stay `—`; on "continue" the assessment resumes at the first `—` row — never re-ask a graded probe.*

## Steps

| Step | Gate | Status | Level | Treatment | Chapter | Exercise | Capstone | Feedback |
|---|---|---|---|---|---|---|---|---|
| 0. Get to the data | cleared | complete | Developing | Compressed | — | done | y | y |
| 1. Locate it | cleared | complete | Developing | Compressed | — | done | y | y |
| 2. Why it's there | unlocked | in_progress | Proficient | Capstone only | — | — | n | n |
| 3. Get the data | locked | todo | — | — | — | — | n | n |
| 4. See the science | locked | todo | — | — | — | — | n | n |

**Gate legend:** `locked` = previous module not cleared · `unlocked` = open, capstone not yet cleared · `cleared` = gate passed, next module open.
**Next action:** Step 1 exercise — load bathymetry array, display with matplotlib, then capstone.

```

## feedback.md

```markdown
# Activity feedback — Riley

<!-- LOCAL + PRIVATE by default (gitignored). Shared with Dr. Soule only with the student's consent.
     One entry appended per activity, at the END of the file (keep chronological order).
     This is how the onramp improves between students. -->

---
## Step 0 — Get to the data   (2026-06-11)

- **Difficulty:** About right — maybe a tiny bit easy since content was familiar; student noted Compressed is the right call (confirming vs. reteaching)
- **Friction / confusing / broke:** vi couldn't run in full-screen mode even via JupyterLab terminal — had to narrate instead of demonstrate. Minor frustration around not being able to *prove* it the same way as other commands.
- **Helped most / would change:** Running actual commands and seeing real output (`head data/plate_boundary.csv` returning real lon/lat numbers) made it feel like real work rather than a quiz — student said this made it stick better.
- **Errors hit (paste any traceback):**
  ```
  (none)
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

---
## Step 1 — Locate it   (2026-06-11)

- **Difficulty:** About right, slightly easy once the `extent=` hint landed; axis-constraint problem was the genuinely challenging part
- **Friction / confusing / broke:** Minor uncertainty about whether `.item()` was necessary; nothing broke
- **Helped most / would change:** The "what happens if you don't constrain the axes" Socratic question before the capstone — student said it made them understand *why* rather than just copying a fix; would keep this pattern
- **Errors hit (paste any traceback):**
  ```
  (none)
  ```
- **Treatment given:** compressed   ·   **Level:** developing
---

<!-- ⟂ END MARKER: keep this as the LAST line. Insert each new entry IMMEDIATELY ABOVE
     this marker (it is the one unique anchor — entries then stay chronological). -->

```
