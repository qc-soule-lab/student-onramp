*AI-generated draft (Claude, Anthropic) — for review.*

# Misconception ledger — aggregated, anonymized

The curriculum-evolution loop (Constitution II). When a student consents to share their local
`misconceptions.md`, its entries are folded in here **anonymized** — no names, no identifying
detail. A misconception that **recurs across students** is the signal to revise the probe, hint
ladder, or chapter mapping that should have pre-empted it.

**How to use:** group by probe; when a gap recurs, file the fix against that probe (or its
chapter mapping in `content/skill_to_chapter.yml`) and record the revision + status here.

| Probe | Skill | Misconception (gap) | Times seen | Revision action | Status |
|---|---|---|---|---|---|
| _example_ `p04_xarray_inspect` | xarray_intro | "one reader for every file — tried `read_csv` on a `.nc`" | — | make step 3 say plainly: netCDF → xarray, not pandas | open |

<!-- Append only AGGREGATED, ANONYMIZED entries. Never paste a student's name or identifying text.
     Delete the _example_ row once real entries exist. -->
