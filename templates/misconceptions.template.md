# Misconceptions log — {{student_name_or_anon}}

<!-- LOCAL + PRIVATE by default (gitignored). Shared with Dr. Soule only with the student's consent.
     One entry per probe scored `partial` or `wrong` — capture the ACTUAL misstep, not just the score.
     Wrong answers are the gold: each reveals a gap to target in the next curriculum revision.
     Append new entries IMMEDIATELY ABOVE the END MARKER (chronological). Descriptive, never judgmental. -->

---
## {{probe_id}} — {{skill}}   (step {{step_n}}, {{timestamp}})

- **Scored:** {{partial | wrong}}   ·   **hint depth:** {{1 | 2}}
- **What the student did/said:** {{their actual answer or approach, in their words — e.g. "tried to open the .nc with pandas read_csv"}}
- **Misconception revealed:** {{the underlying gap — e.g. "expects one reader for every file type; doesn't see .nc as non-tabular"}}
- **Resolved by:** {{which rung / did it land — e.g. "R2 nudge: realized netCDF needs xarray"}}
---

<!-- ⟂ END MARKER: keep this as the LAST line. Insert each new entry IMMEDIATELY ABOVE this marker. -->
